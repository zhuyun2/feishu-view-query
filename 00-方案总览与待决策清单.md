# 方案总览与待决策清单｜飞书多维表格扩展视图插件「卡片视图」

> 本文件是需求方案阶段的**总入口**，汇总 PRD 与技术方案的结论。
> 阶段：需求方案（需求 + 可实现方式 + 部署方案）｜日期：2026-09-20｜汇编：齐活林（交付总监）
>
> ✅ **更新（2026-09-20）**：D1~D10 十条决策**已全部由用户确认**，详见第五节；决策已固化进 `01-PRD.md` 第 11 章，并据此产出开发设计文档 **`03-开发设计文档.md`**。
> ⚠️ 其中 **D3 是实质性变更**：详情区由「槽位表单」升级为 **Word 文档式的 A4 分页排版预览**，为此新增了分页引擎等 4 个模块与 5 条任务（P0-11~P0-15 / T16~T20）。

---

## 一、TL;DR

为飞书多维表格开发一个**新的视图类型**——「卡片视图」：用可视化拖拽把字段排版成卡片墙；点击卡片展开的**详情是一份 A4 纸张样式的分页文档**（类似 Word 打印预览，可缩放、可翻页、可打印导出 PDF）。方案已确认**纯前端可实现、无需自建服务器**，全部代码由飞书平台托管分发，通过企业自建应用发布。

---

## 二、交付概览

| 项 | 内容 |
|---|---|
| 交付状态 | ✅ 需求方案阶段完成（PRD + 技术实现方案 + 部署方案 + **开发设计文档**） |
| 交付范围 | 需求方案与开发设计，**不含代码实现**（按你的要求先确定方案） |
| 产品形态 | 多维表格插件 → **数据表视图（table-view）**，出现在「新建视图」中 |
| 需自建服务器 | ❌ 不需要 |
| 需配置域名白名单 | ❌ 不需要（运行时零外部网络请求） |
| 高等级风险 | 4 条（配置持久化介质、万行级性能、**打印可用性**、**文档排版引擎工作量**），均已给出验证方式与降级方案 |
| 决策点 | ✅ **10 条已全部确认**（见第五节） |
| 实施任务数 | **21 条**（T01~T21；含文档排版引擎 T16~T20），依赖 7 层 |
| 待验证 Spike | **5 项**（V1 打印可用性 / V2 视图筛选可读性 / V3 图片打印 / V4 bridge 跨用户共享 / V5 iframe 内拖拽） |

---

## 三、两个技术硬骨头的结论（原为方案最大不确定性）

### ✅ 结论 1：视图级配置持久化 —— 能满足，且不需要服务端

| 项 | 结论 |
|---|---|
| 首选方案 | `bitable.bridge.setData / getData / onDataChange` |
| 官方语义 | "该自定义数据在**同一文档的同一插件下是共享的**" |
| 是否满足"随视图持久化" | ✅ 满足（随文档持久化，刷新/重进不丢） |
| 是否满足"所有人看到同一套版式" | ✅ 满足（同文档同插件跨用户共享） |
| ⚠️ 关键设计点 | bridge 作用于"**文档 + 插件**"级，**不是视图级**。同一文档可有多个卡片视图，因此必须用 `viewId` 做 key 命名空间：`cbv:config:{viewId}` |
| 降级方案 | `localStorage` —— ⚠️ **仅存本机、无法跨用户共享**，仅当 bridge 不可用时启用并明确提示用户 |
| 不推荐方案 | 写入表内"隐藏配置表"（可共享但需写权限、污染用户数据，违反"不写回"原则） |
| 容量上限 | 官方文档**未承诺** → 按 ≤64KB 体积预算 + checksum 校验 + 损坏回退默认版式 |

### ✅ 结论 2：万行级性能 —— 可满足指标

- ⚠️ **必须弃用** `table.getRecordList()`——官方已标注**不再维护**（性能问题），这也是最容易被踩的坑
- ✅ **改用** `table.getRecordsByPage({ viewId, pageSize ≤ 200, pageToken })`（官方推荐主路径，所有批量取数 API 实际都基于它）
- 有序取数用 `view.getVisibleRecordIdList()`
- 策略：首批 1 页（100~200）→ 首屏只渲染可视区约 30 张 → 滚动到阈值增量取下一页 → LRU 缓存 → 骨架屏过渡
- 虚拟化：行级虚拟化 + 动态高度测量（应对卡片不等高）

---

## 四、方案要点速览

| 维度 | 结论 |
|---|---|
| 架构 | 飞书宿主 → iframe 沙箱内 React 插件 → 官方 SDK → 多维表格数据；**纯前端** |
| 框架 / 语言 | React 18 + TypeScript 5 |
| 构建 | Webpack 5 + `@lark-opdev/block-bitable-webpack-utils`（**平台强制，不可用 Vite**） |
| CLI | `@lark-opdev/cli`（官方强制） |
| 运行时 SDK | `@lark-base-open/js-sdk` |
| 状态管理 | Zustand（草稿态高频更新友好） |
| 拖拽 | dnd-kit（基于**指针事件**，适配 iframe 沙箱；HTML5 拖拽在 iframe 中易失效） |
| 虚拟滚动 | @tanstack/react-virtual（支持动态高度） |
| 组件 / 样式 | Radix UI 无样式基座 + 自研；Tailwind + CSS Modules |
| 测试 | Vitest + React Testing Library + Playwright |
| 工程规模 | 约 60+ 源文件，`src/` 下分 sdk / config / data / fields / state / components / highlight / hooks / i18n / constants / utils / styles 十二层 |
| 发布链路 | **7 步**：`opdev upload` → 后台选版本+图标+介绍 → 保存 → 创建版本 → 确认权限可见范围 → 申请线上发布 → 管理员审核 → 用户安装 |
| 环境隔离 | 推荐"dev/staging/prod 三套企业自建应用"，备选"单应用 + 语义化版本 + 灰度" |

---

## 五、决策结论（D1~D10 已全部确认 · 2026-09-20）

| # | 决策点 | **确认结论** | 影响 |
|---|---|---|---|
| **D1** | MVP 是否只读？ | ✅ **只读**，不写回记录 | 权限基线用免审的 `bitable:app:readonly`，大幅缩短上线周期 |
| **D2** | 悬浮预览范围？ | ✅ P0 简要气泡 / P1 完整浮层 | P0/P1 边界已定 |
| **D3** | 详情展开形式？ | ✅ **右侧抽屉 + Word 式 A4 分页文档排版预览** ⚠️ | **重大变更**：新增分页引擎等 4 模块、5 条任务（P0-11~P0-15 / T16~T20），工作量增加约 30%~35% |
| **D4** | 复制视图时配置？ | ✅ 不自动跟随，首开提示从模板重配 | 配置模型 + 首开引导 |
| **D5** | 条件高亮表达式深度？ | ✅ 单层 AND/OR，不做嵌套 | 规则引擎边界已定 |
| **D6** | 移动端？ | ✅ **本期不支持**，排除到 P2 | 范围收敛 |
| **D7** | 原生筛选/排序对齐？ | ✅ **对齐原生筛选（含排序）**；分组本期不做 | ⚠️ 插件内搜索需读视图筛选条件，列为待验证 V2 |
| **D8** | 国际化范围？ | ✅ MVP 仅简体中文，架构预留 | 文案量收敛 |
| **D9** | 配置容量与超限行为？ | ✅ ≤64KB + checksum + 超限提示 | 保存前拦截 |
| **D10** | 环境隔离方案？ | ✅ dev / staging / prod **各一套企业自建应用** | 3 份后台维护成本 |

---

## 六、两条高等级风险（需在编码前先验证）

| 编号 | 风险 | 应对 |
|---|---|---|
| **R1** | **bridge 的跨用户共享与持久化承诺未在官方文档明示**，实际行为可能因端而异 | ⚠️ **P0 阶段第一件事做 Spike 验证**：用**两个不同账号**打开同一文档同一视图，确认看到的配置是否一致、刷新后是否保留。若失败 → 启用"隐藏配置表"次级方案 |
| **R2** | 万行级性能不达标（首屏 >3s 或滚动 <50FPS） | 分页 + 行虚拟化 + memo + 懒渲染；CI 设性能门禁；提供"精简模式"降级 |

> 其余 9 条风险（WebView 兼容、SDK API 废弃、字段类型新增、权限审批阻塞、宿主 API 变更、拖拽与 iframe 兼容、bridge 容量、并发写冲突、附件资源加载）详见技术方案第 14 章。

---

## 七、文件清单

| 文件 | 内容 | 作者 |
|---|---|---|
| `00-方案总览与待决策清单.md` | 本文件（总入口 + 决策清单） | 齐活林 |
| `01-PRD.md`（v1.1） | 产品需求文档：产品目标、4 类角色 5 个场景、US-1~US-6、P0/P1/P2 需求池、**交互线框与拖拽流程**、**24 类字段支持矩阵**、非功能指标；**第 11 章为决策固化与 D3 详情区需求修订**（新增 P0-11~P0-15） | 许清楚（产品经理） |
| `02-技术方案与部署方案.md` | 18 章技术文档：可行性逐条回应、技术选型、目录树、完整 TypeScript 数据模型、接口抽象、字段渲染器注册表体系、4 张时序图、性能方案、状态机、错误降级、**部署发布全流程 + 上线检查清单**、风险登记册、15 条任务列表与依赖图、依赖包清单、共享约定 | 高见远（架构师） |
| ⭐ `03-开发设计文档.md` | **开发设计文档（本版核心交付，20 章）**：决策基线、双渲染目标架构、**文档排版引擎设计（12 类区块库 / 确定性分页算法 / 打印导出与降级链路）**、v2 全量 TS 数据模型、21 条任务与 4 个里程碑、**分页专项测试用例 10 条**、5 项 Spike 清单、需求追踪矩阵 | 齐活林（交付总监） |
| `class-diagram.mermaid` | 类图（独立图文件，可直接复用） | 高见远 |
| `sequence-diagram.mermaid` | 时序图（独立图文件，可直接复用） | 高见远 |

---

## 八、下一步建议

1. **评审 `03-开发设计文档.md`**（尤其是 §5 文档排版引擎与 §16 任务/里程碑），确认 D3 带来的工作量与分期方式是否符合预期。文档中已标注一条现实风险：**文档排版引擎是本项目最重的部分（约占 30%~35% 工作量）**，建议按里程碑 M3 分两段交付（先支持不可切分块分页，长段落/表格切分算法作为 M3 后半段独立任务）。
2. **先做两个 Spike（写业务代码前）**：
   - **V1 打印可用性**：在飞书客户端/网页版 iframe 内验证 `window.print()` 是否可用、是否只打印纸页 —— 若不可用，D3 的"打印预览"价值会打折，必须提前暴露。
   - **V4 bridge 跨用户共享**：用两个不同账号验证配置是否共享、刷新是否保留 —— 唯一可能推翻"配置全表共享"这一核心假设的点。
3. **前置准备可并行启动**（不依赖编码）：在飞书开发者后台创建 3 套企业自建应用（dev/staging/prod）→ 各添加"多维表格插件 → 数据表视图"能力 → 拿到 **App ID** 与 **BlockTypeID** → 申请 `bitable:app:readonly`（免审，即时生效）→ 确认企业管理员审批通道。
4. **准备联调素材**：一个含多字段类型的测试多维表格 + 1.2 万行测试数据（性能验收基线）。
5. 确认后即可按 **T01~T06** 开工，打通"能渲染出卡片"的最小闭环（对应里程碑 M1）。
