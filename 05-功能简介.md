# 功能简介｜飞书多维表格扩展视图插件 ——「卡片视图」

| 项 | 内容 |
|---|---|
| 产品名称 | 卡片视图（Card View） |
| 产品形态 | 飞书多维表格插件 · **数据表视图**类型（出现在「新建视图」的视图类型列表中） |
| 发布形态 | 飞书企业自建应用（非应用商店上架） |
| 当前版本 | **v1.4.1** |
| 文档日期 | 2026-09-23 |
| 配套文档 | `05-功能简介.md`（本文）、`06-使用说明书.md`（操作手册） |

---

## 1. 一句话定位

> **把「横向拖表格找字段」变成「一眼看清关键信息的卡片墙」** —— 不改变字段、不复制数据，为同一张多维表格定制出最贴合业务阅读习惯的卡片视图。

它不替换你的表，也不搬走你的数据：只是给同一份数据换一副「以业务语言阅读」的面孔。

---

## 2. 它解决什么问题

| 常见困扰 | 卡片视图的做法 | 用户感受到的变化 |
|---|---|---|
| 字段多，要横向拖动才能看到关键列 | 每条记录收敛为一张卡片，关键字段按重要性排进固定槽位 | 一屏看完一批记录，不再横向滚动 |
| 同一张表不同场景想要的版式不同（客户视角 / 盘点视角…） | 同一张表可建多个卡片视图，各自独立配置 | 不必复制表格、不必删字段 |
| 详情要看的东西和列表不一样 | 详情沿用同一套自定义排版，支持扩展为全字段 | 卡片与详情视觉一致，学习成本为零 |
| 异常记录（逾期、超期）靠肉眼扫 | **条件高亮**：按字段值给整卡边框或字段着色 | 异常一眼可见 |
| 排版要给非技术同事用，但改版式要开发 | 可视化**拖拽**排版 + 模板套用 + 撤销 | 业务人员自己就能改 |
| 已有 Word 模板（合同、标签、工单）想直接套用 | **导入 .docx 模板 + 占位符**读表格字段 | 沿用既有模板，不必重画 |

---

## 3. 核心能力一览

### 3.1 阅读侧（所有人可用）

| 能力 | 说明 |
|---|---|
| **卡片视图** | 四槽位卡片：标题 / 副标题 / 属性 / 底部；无值字段自动隐藏、空槽位自动收合 |
| **密度三档** | 紧凑 / 标准 / 宽松 —— 联动卡片宽度、间距、行数与属性区最多行数 |
| **条件高亮** | 按条件让「整卡边框 / 指定字段 / 指定标签」着色，支持 AND、OR 与优先级 |
| **搜索** | 匹配**已放入卡片**字段的显示文本，大小写不敏感 |
| **字段筛选** | 插件内自建筛选器，算子体系与原生筛选一致；支持模糊查找字段；条件即时生效；支持 AND / OR |
| **记录详情** | 点击卡片（或聚焦后按 Enter / 空格）从右侧滑出详情抽屉；单张连续长页，可缩放、全屏、拖宽 |
| **悬浮预览** | 悬停卡片 150ms 后弹出轻量气泡（标题 + 属性区前 4 行） |
| **大表流畅浏览** | 每批 200 条增量加载，滚动到底部自动续载；骨架屏过渡 |

### 3.2 配置侧（需表格编辑权限）

| 能力 | 说明 |
|---|---|
| **可视化拖拽排版** | 从字段池拖字段到槽位，槽位内可拖动排序，拖出即移除；全程实时预览 |
| **卡片模板** | 内置多种版式预设，一键换版（保留已用字段） |
| **主题与密度** | 主色、圆角、阴影、标题字重、字号缩放；密度三档 |
| **文档排版** | 12 类区块（标题/段落/静态文本/键值网格/字段清单/标签行/图片/表格/分隔线/间距/强制分页/页脚元信息）拖拽编排，右侧属性面板逐块精调 |
| **导入 .docx 模板** | 用 `{字段名}` 占位符读表格字段，**保真渲染**到详情；上传即体检，提前暴露字段名写错、循环未闭合等问题 |
| **团队共享** | 配置存于飞书文档级存储（按视图隔离），同一文档同一视图的成员共用一套版式 |

---

## 4. 关键主张与保障

- **数据不搬家**：数据始终留在原多维表格，插件只改变「呈现层」，不产生数据副本与同步风险。
- **只读不污染**：插件**不写入表格数据**，**不改写视图的原生筛选 / 排序**；唯一会写的是插件自有的排版配置。
- **筛选不外泄**：筛选在插件内客户端求值，**绝不下推服务端**，因此不会影响他人看到的原生视图。
- **配置有兜底**：配置损坏时自动回退默认排版、保留原始数据备份，并明确提示「未改动你的表格数据」。
- **权限自适配**：无编辑权限的用户只读浏览（配置入口自动隐藏/禁用）；遇到更高版本插件写入的配置时进入只读模式，不覆盖较新数据。
- **零新增依赖**：产物自包含、无自建后端、无外部 CDN。

---

## 5. 典型场景

1. **客户档案卡**：标题=客户名称，副标题=所属行业/负责人，属性=合同金额/最近跟进时间，底部=状态标签。销售开会前一屏 30 张卡，3 秒定位重点客户。
2. **项目进度墙**：属性区放负责人/截止日期/进度，配置条件高亮「逾期记录卡边变红」，晨会直接投屏。
3. **工单/合同套打信息**：用既有 Word 模板导入，占位符绑定字段，点开卡片即见一份按模板生成的完整单据信息（本期为**信息呈现**，不含打印导出）。

---

## 6. 适用范围与边界

| 维度 | 说明 |
|---|---|
| 适用端 | 桌面端飞书客户端与网页版（移动端完整编辑不在本期范围） |
| 数据规模 | 单表、万行级（每批 200 条增量加载，配合「加载全部并重新筛选」） |
| 定位 | 阅读/浏览增强视图，**不做数据写回、不做图表看板、不做跨表聚合** |
| 模板格式 | 本期支持 **.docx**（xlsx 模板为后续规划） |
| 模板体积 | 单个模板 ≤ **1MB**；受平台存储能力约束，实际可写入量可在插件内一键探测 |
| 打印导出 | **本期不含**（后续规划） |

> 详细操作步骤、模板占位符语法、常见问题与已知限制，见 `06-使用说明书.md`。
