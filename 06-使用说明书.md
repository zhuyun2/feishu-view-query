# 使用说明书｜飞书多维表格「卡片视图」插件

| 项 | 内容 |
|---|---|
| 适用版本 | v1.4.1 |
| 适用对象 | 使用 / 配置卡片视图的业务人员与表格搭建者 |
| 文档日期 | 2026-09-23 |
| 配套文档 | `05-功能简介.md`（产品介绍） |

---

## 1. 快速上手（5 步）

1. **打开视图**：在多维表格「新建视图」中选择**卡片视图**；已创建的可直接从视图列表切换。
2. **首次打开**：插件会按默认模板生成一份配置，并提示「首次打开已按默认模板生成配置，可直接使用或按需调整。」（如需重配可点横幅上的「立即配置」）。
3. **配置排版**（需表格编辑权限）：点工具栏 **`⚙ 配置`** 打开「卡片排版编辑器」——左侧拖字段、中间实时预览、右侧调属性；改完点 **`保存`**。
4. **筛选用**：点工具栏 **`筛选`** → `+ 添加条件` → 选字段、选算子、填值（**即时生效**）→ 点 `完成` 收起面板。
5. **看详情**：点击任意卡片，右侧滑出详情；按 `Esc` 或点 ✕ 关闭。

> 没有编辑权限时，`⚙ 配置` 按钮为禁用态（提示「仅查看权限，无配置入口」），浏览、搜索、筛选、看详情一切照常可用。

---

## 2. 界面总览

### 2.1 顶部工具栏（从左到右）

| 控件 | 作用 | 备注 |
|---|---|---|
| 视图名 | 显示当前视图名称 | 未命名时显示「卡片视图」 |
| 计数 | 当前记录总数 / 筛选命中数 | 文案见 §3.3 |
| 搜索框（🔍） | 按关键词过滤**已加载**记录 | 占位符「搜索记录」，有值时出现 ✕ 清空 |
| `筛选` / `筛选 (n)` | 打开筛选面板；已启用 n 条条件时显示计数并高亮 | 面板内为即时生效，见 §5 |
| `排序` | 说明面板：卡片视图沿用表格视图的**原生排序** | 如需改排序，请回到视图工具栏修改；本插件不改动排序 |
| `刷新` / `刷新中…` | 重新读取数据与配置 | 加载中按钮禁用 |
| `⚙ 配置` | 进入「卡片排版编辑器」 | 无权限或配置来自更高版本时禁用 |
| `⋯` 更多 | `刷新数据` / `清空搜索` / `打开排版编辑器` | —— |

### 2.2 状态行（有筛选或搜索时出现）

常驻一行说明「筛选作用范围」，例如：

- `已在已加载的 200 / 共 12,480 条中筛选，命中 37 条`
- 已加载全部时：`已在全部 12,480 条中筛选，命中 37 条`
- 叠加搜索时追加：`，命中 37 条（叠加搜索后 5 条）`
- 未加载全部时附「未加载全部」标签，并提供按钮 **`加载全部并重新筛选`**（点击后自动翻页至末页，重新给出全量筛选结果；进度显示 `已加载 200 / 共 12,480`）
- 存在未生效条件时追加：`（其中 1 个条件未生效）`

### 2.3 各类提示横幅

| 横幅类型 | 文案要点（逐字） | 能否关闭 |
|---|---|---|
| 首开 / 复制视图 | 「首次打开已按默认模板生成配置，可直接使用或按需调整。」 / 「这是从模板新生成的配置（复制视图不会自动跟随原视图配置），可直接使用或稍后重配。」 | 可关闭（含「从模板重配」「立即配置」） |
| 配置损坏 | 「检测到配置数据损坏，已回退默认排版，并保留了原始数据备份（未改动你的表格数据）。」+ 按钮 `重试读取` | 否 |
| 配置仅本地保存（降级） | 「配置仅本地保存，其他成员看不到你的排版（未满足 US-4 AC2）。」或更具体原因（如「配置存储读取失败，已回退到本地保存，其他成员看不到你的排版。」） | 否（常驻） |
| 更高版本只读 | 「配置由更高版本插件创建，当前为「只读模式」，已忽略未知字段，禁止保存。」 | 否 |

### 2.4 空态与错误态

| 场景 | 标题 | 说明 | 动作 |
|---|---|---|---|
| 无记录 | `暂无记录` | 该视图在当前筛选条件下没有可见记录。可新增记录，或调整视图筛选。 | —— |
| 无可用字段 | `当前视图暂无可用字段` | 请检查字段权限，或在该视图中添加字段后重试。 | `重试` |
| 筛选无结果 | `当前筛选无结果` | 没有记录满足当前筛选条件，可调整或清空筛选。 | `清空筛选` |
| 搜索无结果 | `没有匹配的记录` | 没有找到与关键词匹配的记录，换个关键词试试。 | `清空搜索` |
| 配置未就绪 | `配置尚未就绪` | 正在准备卡片排版配置，请稍候。 | —— |
| 初始化失败 | `加载失败` | 显示「步骤：原因」，并附一段只读诊断信息 | `重试` |

---

## 3. 浏览记录

### 3.1 卡片结构

一张卡片按四个槽位自上而下排列：**标题 / 副标题 / 属性 / 底部**。

- 标题、副标题：多字段时用 ` · ` 连接；标题单行省略。
- 属性：每行「字段名（左）+ 值（右）」；行数受密度与模板上限约束，超出的有值行在末尾显示 `+n`。
- 底部：标签样式。
- **无值字段默认隐藏**（不会显示空白行）；整个槽位都没有内容时该槽位自动收合。
- 字段被删除时，卡片上自动隐藏该行。

### 3.2 密度三档

| 档位 | 卡片最小宽度 | 间距 | 内边距 | 属性区最多行数 |
|---|---|---|---|---|
| 紧凑 | 240px | 12px | 10px | 2 |
| 标准 | 280px | 16px | 16px | 3 |
| 宽松 | 320px | 20px | 20px | 5 |

### 3.3 计数文案

| 情况 | 文案 |
|---|---|
| 未收窄 | `共 12,480 条` |
| 收窄且已加载全部 | `共 12,480 条（已筛选 37 条）` |
| 收窄且未加载全部 | `共 12,480 条 · 已在已加载的 200 条中筛选，命中 37 条` |

> 为什么会有「已加载的 N 条」：为流畅浏览，插件**每批加载 200 条**，滚动到底部附近自动续载（加载时网格底部出现骨架行）。因此筛选默认作用在**已加载范围**内 —— 需要全量结果时点状态行的 `加载全部并重新筛选`。

---

## 4. 搜索

- 位置：工具栏中部搜索框；输入即过滤（无需回车）。
- **匹配范围**：只匹配**卡片排版中已引用**字段的**显示文本**（即你看到的卡片文字），大小写不敏感、子串匹配。
- **不匹配**：未放进卡片的字段；字段的原始 ID / 技术值。
- **叠加关系**：先筛选、后搜索，两者同时生效（AND）。
- **作用范围**：仅已加载记录；不与服务端交互。
- 清空方式：搜索框内 ✕、`⋯` 菜单的「清空搜索」、无结果空态的「清空搜索」按钮。

---

## 5. 字段筛选

### 5.1 面板操作

1. 点工具栏 `筛选` 打开面板（面板头提示：`筛选对已加载记录即时生效`）。
2. 点 `+ 添加条件` 新增一行；每行三步：**选字段 → 选算子 → 填值**。
3. 条件数 ≥ 2 时，面板出现 `与` / `或` 切换（默认 `与`，单层组合，不支持嵌套）。
4. 点 `完成` **只是收起面板** —— 筛选在填写时已经生效（这点与「应用」按钮的直觉不同，故按钮名刻意叫「完成」）。
5. `清空` 一键清除全部条件（无条件时按钮禁用）。

### 5.2 选字段

- 字段下拉支持**模糊查询**：输入关键词即过滤（去首尾空格、大小写不敏感、子串匹配字段名或类型提示）。
- 无匹配时显示 `无匹配字段`。
- 下拉只列出**可筛选**的字段；若当前条件用的字段不支持筛选或已被删除，该字段仍会**置顶显示**并标注 `（不支持筛选）` / `（字段已删除）`。

### 5.3 算子矩阵（按字段类型）

| 字段类型 | 可用算子（按界面顺序） |
|---|---|
| 文本 / 电话 / 链接 / 条码 | 等于、不等于、包含、不包含、为空、不为空 |
| 单选 / 多选 | 等于、不等于、包含、不包含、为空、不为空 |
| 数字 / 自动编号 / 货币 / 评分 / 进度 | 等于、不等于、大于、大于或等于、小于、小于或等于、为空、不为空 |
| 日期 / 创建时间 / 修改时间 | 等于、不等于、**晚于**、**早于**、为空、不为空 |
| 复选框 | 等于 |
| 成员 / 创建人 / 修改人 / 群聊 | 包含、不包含、为空、不为空 |
| 附件 | 为空、不为空 |
| 单向关联 / 双向关联 / 查找引用 | 包含、不包含、为空、不为空 |
| 地理位置 | 包含、不包含、为空、不为空 |
| 公式 | 为空、不为空、包含、不包含 |
| 未收录类型 | 不支持筛选（不出现在字段下拉中） |

**字段类型提示（界面内 ⓘ 悬停可见）**

| 类型 | 提示原文 |
|---|---|
| 日期类 | 仅支持按「某一天」筛选；「晚于」= 次日 0 点及以后，「早于」= 当日 0 点之前 |
| 成员类 | 按成员显示名匹配，同名成员可能误命中 |
| 关联类 | 按关联记录标题文本匹配 |
| 地理位置 | 按地址文本匹配 |
| 公式 | 公式字段结果类型不可知，仅支持空值与文本包含判断 |
| 不可筛 | 该字段类型不支持筛选 |

### 5.4 值输入控件

| 字段类型 | 控件 |
|---|---|
| 文本 / 电话 / 链接 / 条码 / 成员 / 关联 / 查找 / 位置 / 公式 | 单行文本框 |
| 数字 / 自动编号 / 货币 / 评分 / 进度 | 数字框 |
| 日期 / 创建时间 / 修改时间 | 日期选择器 |
| 单选 / 多选 | 下拉（选项来自字段配置，首项 `请选择`） |
| 复选框 | 下拉 `是` / `否` |
| 「为空」「不为空」及附件类型 | 不需要填值（不显示输入框） |

### 5.5 无效条件如何提示（重要）

条件行若不会生效，会整行标红并给出**具体原因**（按优先级）：

| 原因 | 文案 |
|---|---|
| 算子未知 | `该条件不会生效：未知算子「xxx」` |
| 类型 × 算子不匹配（含字段已删除） | `该条件不会生效：所选算子不适用于此字段类型` |
| 值未填写 | `该条件不会生效：尚未填写筛选值` |

同时面板顶部出现汇总：`⚠ 有 N 个条件未生效，已按其余条件筛选`；状态行追加以括号后缀 `（其中 N 个条件未生效）`。

### 5.6 「空值条件不会隐藏数据」（设计保证）

新增条件行**不会预填值**。在你填写之前：

- 该条件被标红提示 `尚未填写筛选值`；
- **卡片数量不变**（不会因为一条空条件把记录全部筛掉）；
- 若**所有**条件都未生效，则视为「不筛选」，显示全部记录，并提示「N 个条件未生效」。
- `为空` / `不为空` 两个算子永远有效，不需要填值。

> 这条规则的意义：宁可筛选暂不生效，也绝不让记录被静默隐藏。

### 5.7 与原生表格筛选的关系

本插件的筛选**完全在插件内计算**，不会写回、也不会改变表格视图的原生筛选设置；两者互不影响。排序则直接沿用原生视图的排序结果。

---

## 6. 查看记录详情

- **打开**：点击卡片任意位置；或卡片获得焦点后按 `Enter` / `空格`。（悬停只弹轻量气泡，不打开抽屉。）
- **悬浮预览**：悬停 150ms 后弹出气泡，内容为标题 + 属性区前 4 行 + 提示 `点击卡片查看详情`。
- **详情内容**：按「文档排版」配置渲染，形态为**单张连续长页**，内容过长时由抽屉内滚动条滚动（不再分页、无页码）。
- **顶部工具**：`适应宽度`、`－`、缩放百分比、`＋`、`全屏` / `退出全屏`、`✕`。
- **抽屉尺寸**：默认 860px，可拖拽在 560px ~ 满宽之间调整（底部提示：`560px ~ 满宽 · 缩放 75%~150%`）。
- **关闭**：`✕` 按钮、点击遮罩、或按 `Esc`（全屏时 `Esc` 先退出全屏，再按一次关闭）；关闭后焦点回到原卡片。
- **切换记录**：抽屉内没有「上一条 / 下一条」，需关闭后点击其它卡片。
- **异常情况**：记录不存在时显示 `记录不存在或已被删除。`；文档无可排版内容时显示 `暂无可排版的文档内容。`；字段类型不支持时该字段显示 `该字段类型暂不支持`；字段已被删除时显示 `—` 占位。

---

## 7. 排版编辑器 · 卡片排版

> 入口：工具栏 `⚙ 配置` → 顶栏 `卡片排版` 标签页。整个编辑器为三栏：**左·字段池 / 中·预览与槽位 / 右·属性**。

### 7.1 拖拽绑定字段

1. 左栏「字段池」把字段分为三组：`已使用` / `未使用` / `不支持类型`。
2. 从字段池拖动字段到中间任一槽位投放区松手即可落位；槽位内可**拖动排序**，chip 上 `✕` 移除。
3. 一个字段**至多出现在一个槽位**；把已在别处的字段再拖过去，等于「移动」而非复制。
4. `不支持类型` 组内的字段无法拖入；点击会提示「`字段名`：该字段类型暂不支持在卡片中呈现」，并可用「查看说明」查看原因。
5. 中间「预览」区使用**样例数据**渲染，提示文字：「预览使用样例数据；实际卡片内容以你的记录为准。」

### 7.2 槽位配置（右栏 · 槽位配置）

- 四个槽位：`标题` / `副标题` / `属性` / `底部`，每个显示当前字段数（如「2 个字段」）。
- `显示该槽位` 复选框：可整体隐藏某槽位。
- `排列`：`横向` / `纵向`（纵向适合清单式副标题）。

### 7.3 卡片模板 / 密度 / 主题

| 分区 | 可配置项 | 选项 |
|---|---|---|
| 卡片模板 | 版式预设 | 套用后整体替换槽位配置，**保留已用字段** |
| 密度 | 密度档位 | `紧凑` / `标准` / `宽松` |
| 主题 | 主色 | 6 色：#3370FF / #245BDB / #14C9C9 / #00B42A / #FF7D00 / #F53F3F |
| 主题 | 圆角 | 4 / 6 / 8 / 12 |
| 主题 | 阴影 | `无` / `弱` / `中` / `强` |
| 主题 | 标题字重 | 400 / 500 / 600 / 700 |
| 主题 | 字号缩放 | 0.85× ~ 1.3× |

### 7.4 条件高亮

- 点 `+ 添加规则` 新增规则；每条规则可配：`启用规则`、`规则名称`、`目标`（`整卡边框` / `字段：xxx` / `标签：xxx`）、`满足`（`全部满足` / `任一满足`）、若干条件行、`样式`（5 色）、`优先级`（0–999）。
- 条件行 = 字段 + 运算符 + 值；运算符：等于、不等于、大于、大于等于、小于、小于等于、包含、不包含、为空、不为空、早于、晚于（后两者用于日期）。
- 空态提示：「尚未配置高亮规则。可基于字段值让卡片边框或字段着色。」

### 7.5 草稿、撤销与保存

| 操作 | 行为 |
|---|---|
| 改动 | **实时写入草稿**并实时预览，顶栏出现 `未保存` 标记 |
| `撤销` | 逐步回退（最多 50 步）；无可撤销时提示「没有可撤销的操作」 |
| `保存` | 保存到配置并提示「已保存」，随后关闭编辑器 |
| `取消` / 关闭 | 若有未保存改动，弹出确认：「放弃未保存的修改？」/「你有尚未保存的排版改动，关闭后将丢失这些修改。」→ `继续编辑` / `放弃并关闭` |

> 「卡片排版」与「文档排版」两套草稿互不影响，「取消」只丢弃当前标签页的改动。

---

## 8. 排版编辑器 · 文档排版

> 顶栏 `文档排版` 标签页。「文档排版」即**卡片详情的排版** —— 你在详情里看到的就是这里配置的。

### 8.1 两种模板来源

编辑器最上方「模板来源」横栏二选一：

| 来源 | 说明 |
|---|---|
| `可视化排版` | 用左侧区块库拖拽编排（下方 §8.2 ~ §8.4） |
| `导入 docx` | 上传 Word 模板并用占位符读字段（见 §9） |

切换来源**只改变详情用哪套模板**，已上传的模板会原样保留。

### 8.2 区块库（12 类，5 组）

| 分组 | 区块 | 用途摘要 |
|---|---|---|
| 文本 | 标题 / 段落 / 静态文本 | 标题层级（可静态或绑定字段）、段落（可绑定字段）、支持 `**粗体**`、`*斜体*`、换行与 `- 列表` 的富文本 |
| 字段 | 键值网格 / 字段清单 / 标签行 | 最常用的「字段名: 值」网格、逐项字段清单、标签/徽章行 |
| 媒体 | 图片 / 表格 | 附件取图（首张/全部/指定序号，可设宽高与图注）、表格（列、行来源=当前记录或关联记录、表头、斑马纹、最大行数） |
| 结构 | 分隔线 / 间距 / 强制分页 | 线宽线型、空白高度、在此处强制结束当前页 |
| 元信息 | 页脚元信息 | 创建人 / 创建时间 / 修改人 / 修改时间 / 记录 ID |

### 8.3 画布操作

- **从左侧拖入**区块：区块之间与首尾都有插入位置（画布底部提示：`从左侧拖入区块 · 拖动区块可排序 · 点击区块编辑属性`）。
- **拖动排序**：拖动已有区块在区块间移动。
- **点击选中**：点区块即选中，右栏切换到它的属性。
- **删除**：在右栏属性面板底部点 `删除该区块`。
- 状态栏显示 `已用 n 块 / 插入槽 n+1`。
- 画布中的条件显隐区块若在样例数据下不渲染，会提示「该区块在当前示例数据下不渲染」。

### 8.4 右侧属性面板（三折叠）

1. **区块属性**：未选中时提示「未选中区块 / 在中间画布中点选一个区块，即可编辑它的属性。」
   - 通用（所有区块）：`分页行为`（`允许跨页` / `整体不跨页`）、`对齐`（左/居中/右/两端）、`上间距`、`下间距`、`条件显隐`（仅「首字段非空」时显示该块）、`备注`（仅编辑器内部标识）。
   - 专属：各类区块有各自选项（列数、字段行、标签列宽、斑马纹、隐藏空行、主色……），字段行支持 `添加一行` / `移除`。
2. **页面设置**：`纸张`（A4 / A5 / Letter）、`朝向`（`纵向` / `横向`）、`页边距 (mm)`（上/右/下/左）。
   > 说明：**页眉 / 页码 / 页脚配置已按需求移除**；纸张与页边距仍然影响正文宽度，请按需设置。
3. **主题**：字体（系统默认/宋体/楷体/等宽）、基准字号、行高、字阶（H1/H2/H3 倍率）、标题字重、区块间距、首行缩进、主色、正文色/弱化色/分隔线色。

---

## 9. 导入 .docx 模板

### 9.1 上传与规则

1. 「模板来源」选 `导入 docx` → 点 `选择 .docx 模板`。
2. 规则：
   - 仅接受 **.docx**（其它类型提示：`仅支持 .docx 模板文件（当前文件：xxx）`）。
   - 单个模板 **≤ 1MB**（超限提示形如：`模板 33.0KB（33792 字节），超出上限 1024.0KB（1048576 字节），请压缩模板后重试`）。
   - 上传后立即**体检**（不等保存），体检结果直接显示在下方。
3. 已上传后按钮变为 `重新选择 .docx`；当前模板以 `文件名（体积）` 形式展示（如 `合同模板.docx（28.4KB）`）。

### 9.2 占位符语法

| 写法 | 含义 |
|---|---|
| `{字段名}` | 插入该字段的值（按数据表格式显示） |
| `{#字段名}…{/字段名}` | 循环/明细段（**本期暂不可用**，见 §12） |

规则与边界：

- 字段名**前后空格会被忽略**；**大小写敏感**；**精确匹配**，不做模糊/近似匹配 —— 写错一个字就会进「未匹配」而不是猜。
- 无法配对的 `{` 或 `}`、空标签 `{}` / `{  }`、空名标签 `{#}` / `{/}` 一律**按普通文本处理**，不会吞掉你文档里的字符。
- 不支持 `^`（反向段）、`-`（局部段）、`=`（换分隔符）等高级前缀写法；这类内容会被当作「名字以此开头的占位符」并在体检中报「未匹配」。
- **字段重名**：若表中有两个同名字段，该占位符**不会**取第一个，而是留空并在体检中报「重名冲突」。

### 9.3 体检结果怎么看

| 区域 | 文案 | 含义与处理 |
|---|---|---|
| 匹配成功 | `匹配字段 N 个` | 正常（绿色） |
| 未匹配 | `模板里写了、但没有对应字段的占位符（导出后是空白，请核对字段名）` + 占位符原文 | 逐条核对字段名拼写/空格/大小写 |
| 重名冲突 | `字段重名导致无法确定用哪个字段（已留空，不会猜）` / `源字段重名冲突（无法确定用哪个字段）` + 候选字段 id | 请先重命名表格字段，使名称唯一 |
| 循环未闭合 | `循环未闭合（写了开始标签，但没有对应的结束标签）` + `{#x} 缺少 {/x}` | 补上结束标签 |
| 多余结束标签 | `多余的循环结束标签（没有对应的开始标签）` + `{/x} 缺少 {#x}` | 删掉或用错位处修正 |
| 未引用字段 | `模板未引用的字段：A、B` | 提示性信息（弱化色），不影响导出 |

> 体检的价值：**提前告诉你哪些占位符会渲染成空白** —— 否则文档看起来「有内容」，但那些位置悄悄是空的。

### 9.4 检测存储容量（可选，排障用）

- 入口：「导入 docx」分支的 `检测存储容量` 按钮。悬停说明：「在专用 key 上试写不同体积，实测当前平台可写入的最大模板字节数（不影响你的数据）」。
- 行为：逐档试写 32KB / 64KB / 128KB / 256KB / 512KB / 1MB，遇首个失败即停止；结果形如：

  ```
  存储容量探测
  最大可写入 64.0KB（65536 字节）
  32.0KB（32768 字节）：成功
  64.0KB（65536 字节）：成功
  128.0KB（131072 字节）：失败（set block entity error）
  ```
- **安全说明**：探针只读写专用 key（`cbv:probe:` 前缀）并在结束时清理，**绝不触碰你的排版配置与其它数据**。

### 9.5 详情侧渲染说明

- 来源为 `导入 docx` 时，详情按模板**保真渲染**：
  - 生成中显示 `正在生成文档…`；
  - 页面的宽度与页边距**以模板自带的为准**（这是刻意设计：套用插件纸张设置等于改版你的 Word 模板）；
  - 缩放只做等比视觉缩放，不改变模板版式。
- 若配置里没有模板数据，详情提示：`未找到导入的模板：当前文档来源为「导入的 docx」，但配置里没有模板数据。请在「文档排版」中重新上传模板。`

---

## 10. 配置保存与共享

| 项 | 说明 |
|---|---|
| 存储位置 | 插件配置按**视图**隔离（同文档不同视图互不覆盖） |
| 共享范围 | 飞书存储可用时，**同一文档、同一视图的成员共用一套配置**；若降级为本地保存，则**仅本机可见**（页面顶部会有常驻提示） |
| 体积上限 | **64KB**；接近上限会提示，超出则拒绝保存：`配置体积约 72KB，已超过 64KB 上限，请精简区块或高亮规则后再保存` |
| 模板字节 | 不计入 64KB：模板另行分块存储（主配置只留引用与完整性校验信息） |
| 无权限 | `⚙ 配置` 禁用；即便进入编辑也无法保存（提示「仅查看权限，无法保存配置」） |
| 更高版本配置 | 进入只读模式，保存被拒绝：`当前配置由更高版本的插件写入，为避免覆盖较新数据，本版本仅支持只读浏览，暂不能保存。` |
| 配置损坏 | 回退默认排版 + 保留原始数据备份，横幅提示「未改动你的表格数据」并提供 `重试读取` |

---

## 11. 常见问题（FAQ）

**Q1：为什么筛选只筛了「已加载的 200 条」？**
为流畅浏览，插件分批加载（每批 200 条）。需要全量结果时，点状态行的 `加载全部并重新筛选`。

**Q2：我点了筛选面板里的「完成」，为什么筛选没有应用？**
「完成」只是收起面板 —— 筛选在你填写时就已即时生效。所以按钮名才不叫「应用」。

**Q3：我新加了一条筛选条件还没填值，卡片怎么没变化？**
这是**故意设计**：未填值的条件被判为「未生效」并跳过，记录保持可见（你会看到行内红字「尚未填写筛选值」）。填上值后才参与筛选。

**Q4：条件行标红了，什么意思？**
说明该条件当前**不会生效**，红字会告诉你具体原因：值没填 / 算子不适用此字段类型 / 算子未知。此时其余条件仍照常生效，状态行会注明「其中 N 个条件未生效」。

**Q5：搜索为什么搜不到某个字段的内容？**
搜索只匹配**已放入卡片**的字段显示文本。若该字段没被拖进任何槽位，请先把它加进卡片。

**Q6：详情里的字体/纸张和我配置的不一样？**
如果你用的是**导入的 docx 模板**，详情会以模板自带的页面尺寸与页边距渲染（保真优先）；只有「可视化排版」来源才应用 §8.4 的页面设置。

**Q7：模板里的字段没显示出来（位置是空白）？**
去看「模板体检」的「未匹配」列表 —— 大概率是字段名拼写/空格/大小写对不上；也可能是表格里有重名字段（会明确报重名冲突）。占位符按字段名**精确匹配**，不会猜。

**Q8：导入大模板很慢或失败怎么办？**
先点 `检测存储容量` 看当前平台可写入到多大（常见档位边界是 64KB）。若失败提示里带 `（可能是平台单键存储上限导致，请压缩模板后重试）`，请压缩模板（例如去掉大图、精简样式）后重试。

**Q9：为什么别人看不到我改的排版？**
看页面顶部是否有「配置仅本地保存」提示 —— 说明当前环境的飞书存储不可用，配置降级保存在本机。请在**飞书客户端**中打开该文档重试。

**Q10：卡片上的字段比我配的少 / 多出一个「+2」？**
属性区受密度档位（紧凑 2 / 标准 3 / 宽松 5 行）与模板上限共同约束，超出的**有值行**会被折叠为 `+n` 提示。调大密度或改用属性更多的模板即可。

**Q11：这个插件会不会改我的表格数据或原生筛选？**
不会。插件**只读**表格数据，**不写回**数据，也**不改写**视图的原生筛选/排序；唯一会写入的是插件自己的排版配置。

---

## 12. 已知限制（请留意）

1. **循环/明细段占位符暂不可用**：`{#字段名}…{/字段名}` 当前未接线子记录读取，渲染时会展开为空（体检不会因此报错）。明细行、一对多子表建议暂改为逐个字段展示。
2. **打印导出未包含在本期**：详情为屏幕阅读形态，不含打印/导出 PDF 能力（后续规划）。
3. **xlsx 模板未支持**：本期模板仅 .docx（后续规划）。
4. **页眉 / 页码 / 页脚不再可配**：详情已改为单张连续长页；历史配置中的相关字段会保留但不再影响显示。
5. **详情抽屉不能在记录间切换**：没有「上一条/下一条」，需关闭后另点卡片。
6. **筛选/搜索作用于已加载范围**：需要全量请使用 `加载全部并重新筛选`。
7. **单一 AND/OR 层级**：筛选条件支持一层「与/或」，不支持条件嵌套分组。
8. **悬浮预览内容有限**：仅标题 + 属性区前 4 行。
9. **排序不可在插件内调整**：沿用原生视图排序（插件内只提供说明）。
10. **部分文案细节待优化**：
    - 模板体积超限提示中的上限写作 `1024.0KB`（而非 `1.0MB`）；
    - 模板写入失败的提示会复用时出现「读取模板文件失败：写入模板块失败：…」的双重前缀；
    - 工具栏「排序」说明面板中会出现字面星号（`**原生排序**`）。
    以上均为文案层面问题，不影响功能，已记录待修。
