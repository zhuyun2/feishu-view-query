# PRD｜飞书多维表格扩展视图插件 ——「卡片视图」（Card View）

> 产品形态：多维表格插件 → 数据表视图（table-view），作为「新建视图」中的一个新视图类型出现。

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 文档名称 | 飞书多维表格扩展视图插件 ·「卡片视图」产品需求文档 |
| 版本 | v1.0 |
| 日期 | 2026-09-20 |
| 作者 | 许清楚（产品经理） |
| 评审人 | 齐活林（交付总监）、架构师 |
| 状态 | 待评审（Draft） |
| 适用范围 | 飞书企业内部自建应用（多维表格插件 / 数据表视图） |

### 1.1 原始需求复述

开发一个飞书多维表格的扩展视图插件，插件功能主要是能自定义排版数据表格中的字段，生成一个**卡片视图**；通过点击卡片或悬浮能展开**记录详情**，详情信息展示按**自定义排版格式**显示。需先确定需求方案，方案包括可实现方式、后续部署方案等。

### 1.2 前提约束（已与用户确认，本 PRD 直接作为前提）

| 编号 | 约束 | 说明 |
|---|---|---|
| C1 | **发布形态**：企业内部自建应用 | 不走上架飞书应用商店的公开路径；未来可上架作为 P2 演进项 |
| C2 | **数据规模**：单表、万行级 | 加载体验与性能感知是硬需求，不能假设"无限快" |
| C3 | **自定义排版程度**：可视化拖拽配置 | 用户可拖拽字段到卡片不同区域、实时预览、配置持久化保存 |
| C4 | **交互**：点击 或 悬浮 均可展开详情 | 详情展示同样遵循自定义排版格式 |

### 1.3 平台技术事实（依据飞书开放平台官方文档，作为实现前提，不作为技术选型结论）

1. **产品形态**：多维表格插件，类型包含数据表视图（table-view）、记录视图、自动化操作、字段等；本产品属于**数据表视图**类型，会出现在多维表格「新建视图」的视图类型列表中。
2. **创建流程**：飞书开发者后台 → 创建企业自建应用 → 添加应用能力 → 多维表格插件 → 数据表视图 → 记录 **App ID** 与 **BlockTypeID**。
3. **网络请求**：插件运行时若需发起网络请求，必须在开发者后台配置**服务器域名白名单**。
4. **运行环境**：插件运行于飞书客户端/网页内的 **iframe 沙箱**，通过官方 JS SDK 读写当前多维表格的数据与元数据；官方 SDK 包为 `@lark-base-open/js-sdk`，开发脚手架另提供 `@lark-opdev/block-bitable-api`。
5. **权限**：`bitable:app`（查看、评论、编辑和管理多维表格）、`bitable:app:readonly`（查看、评论和导出多维表格），以**用户身份权限 user_access_token** 开通。
6. **关键机会点**：视图插件本身就是一个**新的视图类型**——「该视图下用户看到什么」完全由插件自定义渲染。

---

## 2. 产品目标与价值

### 2.1 一句话定位

> 把「横向拖表格找字段」变成「一眼看清关键信息的卡片墙」——不改变字段、不复制数据，为同一张多维表格定制出最贴合业务阅读习惯的卡片视图。

### 2.2 价值主张

| 编号 | 价值主张 | 说明 |
|---|---|---|
| V1 | **信息密度可控** | 每条记录收敛为一张卡片，按业务重要性排序字段，一屏看清关键信息，消除横向滚动 |
| V2 | **零门槛自定义** | 可视化拖拽排版，业务人员无需开发即可决定「哪些字段、出现在卡片哪个位置、用什么样式」 |
| V3 | **详情即所配** | 点击/悬浮展开的详情沿用同一套自定义排版，卡片与详情视觉一致，学习成本为零 |
| V4 | **配置即视图** | 配置随视图持久化，团队共享同一套阅读口径，新成员打开即用 |
| V5 | **数据不搬家** | 数据仍留在原多维表格，本插件只改「呈现层」，不产生数据副本与同步风险 |

### 2.3 非目标（明确不做）

| 编号 | 非目标 | 原因 |
|---|---|---|
| NG1 | MVP 阶段不做数据写回（增删改） | 定位为「阅读/浏览增强视图」，降低权限与协作冲突风险 |
| NG2 | 不做跨表/多表聚合视图 | 超出「单表视图」形态，平台约束下复杂度高 |
| NG3 | 不做图表 / 仪表盘 / 统计看板 | 与「卡片阅读」定位不同，避免功能漂移 |
| NG4 | 不做移动端完整编辑能力 | MVP 聚焦桌面端飞书客户端与网页 |
| NG5 | 不做插件内独立账号体系与鉴权 | 完全依托飞书用户身份与权限 |
| NG6 | 不对外开放配置模板市场 | 企建应用定位，未来演进再议 |

---

## 3. 目标用户与使用场景

### 3.1 角色画像

| 角色 | 典型身份 | 核心诉求 | 痛点 |
|---|---|---|---|
| R1 一线业务人员 | 销售 / 客服 / 交付执行 | 快速看清「我这条记录」的关键信息 | 表格字段多，横向拖动才能看到关键字段 |
| R2 表格搭建者 / 业务运营 | 业务运营、部门助理 | 为不同团队 / 不同场景定制不同阅读视图 | 每换一个场景就要复制表格并删字段，维护成本高 |
| R3 团队管理者 | 主管、项目负责人 | 一屏浏览全量记录的关键指标，快速判断 | 在千行表里找异常记录靠肉眼扫 |
| R4（次要）新成员 / 外部协作方 | 临时查阅者 | 无需理解表结构即可阅读 | 不懂字段含义，需要「以业务语言呈现」 |

### 3.2 真实场景故事

1. **S1 客户档案卡（R2 搭建 + R1 使用）**：运营为「客户管理表」建了一个卡片视图，卡面只有「客户名称（标题）+ 所属行业 / 负责人（副标题）+ 合同金额 / 最近跟进时间（属性区）+ 状态色条（底部）」。销售开会前打开该视图，一屏 30 张卡，3 秒定位重点客户。
2. **S2 项目进度墙（R3 管理者）**：项目负责人为「任务表」配置卡片视图，标题 = 任务名、属性区 = 负责人 / 截止日期 / 进度，并配置条件高亮：逾期记录卡边变红。每日晨会直接投屏浏览。
3. **S3 工单详情速查（R1 一线）**：客服接到电话后按客户名搜索，悬浮卡片即预览「联系方式 + 最近工单摘要」，无需点开即可回答问题；需要看全部沟通记录时点击卡片，从右侧滑出详情抽屉。
4. **S4 多场景复用（R2 运营）**：同一张「设备资产表」，运营配置了「巡检视图（突出位置 + 巡检人）」与「盘点视图（突出资产编号 + 状态）」两个卡片视图，无需复制数据。
5. **S5 万行大表浏览（R1/R3）**：市场活动表近 1.2 万行，用户打开卡片视图，首屏 1.5 秒内可见首批卡片，向下滚动平滑加载，不卡顿。

---

## 4. 用户故事（US）与验收标准

### US-1 卡片视图渲染
**作为**一线业务人员，**我希望**把当前多维表格切换成一个卡片视图，**以便**一屏看清每条记录的关键信息，不用横向拖表格。
- AC1：在「新建视图」中可见「卡片视图」这一类型；创建后自动成为当前视图。
- AC2：默认卡片渲染须在视图打开后 **3 秒内**出首屏（万行级数据，取前 N 条）。
- AC3：卡片按配置的槽位顺序展示字段；未配置时使用系统默认模板（首个字段为标题 + 其余字段为属性区）。

### US-2 拖拽式排版编辑
**作为**表格搭建者，**我希望**用拖拽把字段放进卡片的不同区域并实时看到效果，**以便**不用开发就能定制版式。
- AC1：编辑态下左侧展示字段池，卡片划分为 标题区 / 副标题区 / 属性区 / 底部区 四个槽位。
- AC2：从字段池拖拽字段到任一槽位，松手即落位并实时预览；支持槽位内排序、拖出移除。
- AC3：同一字段可放置于多个槽位；同名槽位内重复放置给出提示。
- AC4：编辑操作支持「保存」与「取消」；取消后回到上次保存态。

### US-3 卡片展开详情
**作为**一线业务人员，**我希望**点击或悬浮卡片就能看记录详情，**以便**快速获取完整信息而不离开视图。
- AC1：点击卡片任意非交互元素区域 → 展开详情。
- AC2：悬浮（Hover）卡片 → 出现预览浮层（P1 起为完整能力，P0 保底为简要信息气泡）。
- AC3：详情内容按同一套自定义排版格式渲染（槽位结构一致，字段范围可扩展为「全部字段」）。
- AC4：详情支持关闭（Esc / 点击遮罩 / 关闭按钮），关闭后焦点回到原卡片。

### US-4 配置持久化
**作为**表格搭建者，**我希望**排版配置被保存下来并被团队共享，**以便**所有人打开这个视图看到的都是同一套版式。
- AC1：配置随视图保存，重新打开视图 / 刷新页面后配置不丢失。
- AC2：同一视图所有有权限用户看到相同配置。
- AC3：仅具备视图编辑权限的用户可修改配置；无权限用户进入浏览态。

### US-5 字段类型适配
**作为**一线业务人员，**我希望**不同类型字段（人员、附件、评分、单选等）在卡片上都有得体的呈现，**以便**信息不丢失。
- AC1：P0 字段类型在卡片区与详情区均可正确渲染，不出现原始 ID 或 JSON 串。
- AC2：未支持类型以降级方式展示（提示「该字段类型暂不支持」或纯文本兜底），不导致卡片崩溃。

### US-6 万行级性能感知
**作为**用户，**我希望**大表下滚动和展开都流畅，**以便**长时间浏览不疲劳。
- AC1：滚动帧率目标 ≥ 50 FPS（常规办公机型）。
- AC2：快速滚动时卡片内容先出骨架屏，字段值渐进填充。
- AC3：悬浮预览有 150ms 延迟触发，避免鼠标划过时浮层乱跳。

---

## 5. 需求池

### 5.1 P0（MVP 必须）

| 编号 | 需求 | 描述 | 验收要点 |
|---|---|---|---|
| **P0-01** | 卡片视图基础渲染 | 作为新视图类型注册；卡片按槽位布局渲染记录 | 新建视图可见可选；打开即渲染首屏；默认模板可用 |
| **P0-02** | 卡片槽位结构 | 定义 标题区 / 副标题区 / 属性区 / 底部区 四个槽位 | 四槽位可独立承载字段；空槽位不占位或按规则收合 |
| **P0-03** | 拖拽式排版编辑器 | 字段池 + 拖拽落位 + 槽位内排序 + 移除 | 拖拽可用、可排序、可移除；操作有明确落位反馈 |
| **P0-04** | 实时预览 | 配置变更即时反映到预览卡片 | 修改后 ≤300ms 内预览刷新，无需手动刷新 |
| **P0-05** | 配置持久化保存 | 保存到视图级存储，跨会话保留 | 刷新 / 重进后配置一致；保存、取消语义清晰 |
| **P0-06** | 卡片展开详情 | 点击卡片展开详情，详情遵循同一排版 | 详情渲染正确；关闭交互完整；焦点管理正确 |
| **P0-07** | 字段类型适配基础集 | 文本、多行文本、数字、货币、单选、多选、日期、复选框 | 渲染正确，无原始值外泄；异常降级可用 |
| **P0-08** | 浏览态 / 配置态切换 | 两种状态明确分离，权限驱动 | 有权限者可进入配置态；浏览态无编辑入口 |
| **P0-09** | 基础性能与虚拟化 | 万行级首屏与滚动优化 | 首屏 ≤3s；滚动 FPS ≥50；分页 / 虚拟滚动生效 |
| **P0-10** | 空态与异常态 | 无记录 / 无字段 / 加载中 / 字段被删除 | 各状态有明确占位与引导文案 |

### 5.2 P1（重要）

| 编号 | 需求 | 描述 | 验收要点 |
|---|---|---|---|
| P1-01 | 悬浮预览 | 悬浮卡片弹出简要预览浮层 | 延迟 150ms 触发；不遮挡关键信息；可关闭 |
| P1-02 | 多种布局模板 | 预设多种卡片版式模板（紧凑 / 标准 / 大图 / 清单式） | 一键套用模板；套用后可继续微调 |
| P1-03 | 样式主题 | 卡片圆角 / 阴影 / 主色 / 字号等主题项 | 主题项生效于卡片与详情；可恢复默认 |
| P1-04 | 卡片尺寸与密度 | 卡片宽高、每行数量、间距可调 | 调节实时预览；不同屏宽自适应 |
| P1-05 | 条件高亮 / 规则着色 | 按字段条件设置卡片边框 / 标签 / 文字颜色 | 规则可增删改；命中规则视觉明确 |
| P1-06 | 扩展字段类型适配 | 人员、附件、评分、进度、电话、邮箱、超链接、公式、查找引用 | 各类型渲染符合规范；有降级兜底 |
| P1-07 | 详情区字段扩展 | 详情可展示超出卡片槽位的更多字段 | 详情字段范围可单独配置 |
| P1-08 | 搜索与筛选联动 | 视图内搜索记录、按字段筛选 | 与多维表格原生筛选行为一致 |

### 5.3 P2（进阶）

| 编号 | 需求 | 描述 | 验收要点 |
|---|---|---|---|
| P2-01 | 分享 / 导出 | 卡片视图导出为图片 / PDF，或生成分享链接 | 导出清晰可用；分享受权限约束 |
| P2-02 | 多端适配（移动端） | 飞书移动端下卡片布局自适应 | 移动端可浏览；编辑能力可降级 |
| P2-03 | 配置导入导出 | 配置以文件 / 文本形式导出并在其他视图导入 | 配置可迁移；版本兼容处理 |
| P2-04 | 行列权限联动 | 遵循多维表格高级权限，按用户裁剪可见字段 / 记录 | 无权限字段不泄露；记录不可见则不渲染 |
| P2-05 | 未来上架商店 | 从企建应用演进为可上架应用商店的插件 | 满足上架合规与审核要求 |
| P2-06 | 国际化（中英） | 插件界面文案支持中英切换 | 跟随飞书语言设置 |

---

## 6. 交互设计稿

### 6.1 视图整体布局分区（文字线框）

```
┌──────────────────────────────────────────────────────────────────────────┐
│ ① 视图工具栏 Toolbar                                                       │
│  [视图名: 客户卡片视图 ▾]  [搜索 🔍]  [筛选 ▾]  [排序 ▾]    │ [⚙ 配置] [保存] │
├──────────────────────────────────────────────────────────────────────────┤
│ ② 卡片区 Card Grid（虚拟滚动容器，自适应列数）                              │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐              │
│  │ 标题区      │ │ 标题区      │ │ 标题区      │ │ 标题区      │              │
│  │ 副标题区    │ │ 副标题区    │ │ 副标题区    │ │ 副标题区    │              │
│  │ ─────────  │ │ ─────────  │ │ ─────────  │ │ ─────────  │              │
│  │ 属性区      │ │ 属性区      │ │ 属性区      │ │ 属性区      │              │
│  │ 属性区      │ │ 属性区      │ │ 属性区      │ │ 属性区      │              │
│  │ 底部区      │ │ 底部区      │ │ 底部区      │ │ 底部区      │              │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘              │
│  ... 无限滚动，滚动到底部按需加载下一批（骨架屏过渡）                        │
├──────────────────────────────────────────────────────────────────────────┤
│ ③ 配置态入口（点击工具栏「⚙ 配置」进入「沉浸式三栏编辑模式」，见修订 R1/11.2） │
│  ┌──────────────────────────────┐                                         │
│  │ ▸ 字段池 Field Pool（可拖拽）  │  ── 拖入 ──▶  卡片槽位                   │
│  │   ☰ 客户名称                  │                                         │
│  │   ☰ 所属行业                  │                                         │
│  │   ☰ 合同金额                  │                                         │
│  │   ☰ 负责人                    │                                         │
│  │   ☰ ...                       │                                         │
│  ├──────────────────────────────┤                                         │
│  │ ▸ 槽位配置（标题/副标题/属性/底部）│                                       │
│  │ ▸ 布局模板 / 样式主题 / 尺寸密度  │                                        │
│  │ ▸ 条件高亮规则                  │                                         │
│  ├──────────────────────────────┤                                         │
│  │        [ 取消 ]   [ 保存 ]     │                                         │
│  └──────────────────────────────┘                                         │
└──────────────────────────────────────────────────────────────────────────┘
```

**卡片四槽位定义**

| 槽位 | 位置 | 建议承载字段 | 渲染建议 |
|---|---|---|---|
| 标题区 Title | 卡片最上方，视觉权重最高 | 主标识字段（客户名 / 任务名） | 加粗、单行截断、可配主色 |
| 副标题区 Subtitle | 标题正下方 | 分类 / 归属（行业、负责人、部门） | 次级字号、弱化颜色、多字段以分隔符相连 |
| 属性区 Attributes | 卡片主体，可多行 | 数值 / 状态 / 时间等结构化信息 | 「字段名: 值」两列对齐（标签 + 值） |
| 底部区 Footer | 卡片最下方，固定贴底 | 状态标签、次要时间、操作提示 | 支持色块 / 标签样式 |

### 6.2 拖拽排版编辑器交互流程

**分步流程**
1. **进入编辑态**：浏览态点击工具栏「⚙ 配置」→ 配置抽屉从右侧滑出；卡片区整体进入编辑态，每张卡片槽位显示浅色虚线边框与槽位名称标签。
2. **字段池呈现**：抽屉内列出当前视图**可见且已支持**的字段；已被放置的字段标注「已使用」（仍可重复拖入其他槽位）；不支持的字段置灰并悬浮提示原因。
3. **拖拽落位**：按住字段 → 拖动出现半透明「拖拽幽灵（drag ghost）」→ 卡片四槽位高亮为「可落位」态 → 落在某槽位时显示**插入指示线**标识新字段将插入的位置 → 松手即落位。
   - **落位规则**：同槽位内按落位顺序自上而下排列；拖到槽位空白 = 追加到末尾；拖到已有字段之间的指示线 = 插入该位置。
4. **槽位内操作**：每个已放置字段右侧出现「⋮⋮ 拖柄 / ✕ 移除」；拖柄重排、✕ 移除；移除后字段回到池中「未使用」态。
5. **实时预览**：所有变更（增 / 删 / 排序 / 改样式）即时反映到卡片区与抽屉内的「预览样例卡」；样例卡固定使用 1 条代表性记录（首条有值记录）。
6. **保存 / 取消**：
   - 保存 → 配置写入视图级存储，退出编辑态，卡片区渲染更新。
   - 取消 → 回滚到进入编辑态前的配置快照，退出编辑态。
   - 有未保存修改时关闭抽屉 → 二次确认「放弃未保存的修改？」。

**编辑态元素状态对照**

| 元素 | 浏览态 | 编辑态 |
|---|---|---|
| 卡片槽位边框 | 无 | 浅色虚线 + 槽位名标签 |
| 字段池 | 不可见 | 抽屉内可见，可拖拽 |
| 拖拽落位高亮 | 无 | 目标槽位高亮 + 插入指示线 |
| 槽位内字段控件 | 无 | 「⋮⋮ 拖柄 / ✕ 移除」 |
| 配置抽屉 | 收起 | 展开 |
| 卡片点击展开详情 | 生效 | **禁用**（避免与拖拽冲突，点击卡片不展开） |

### 6.3 卡片展开详情：点击展开 vs 悬浮预览

| 维度 | 点击展开（P0） | 悬浮预览（P1） |
|---|---|---|
| 触发方式 | 单击卡片非交互区域 | 鼠标悬浮卡片 ≥150ms |
| 触发区域 | 整张卡片（排除卡内按钮 / 链接 / 标签等交互元素） | 整张卡片 |
| 展开形式 | **右侧滑出详情抽屉**（默认宽 **860px**，可拖宽 560px~满宽、支持全屏；容纳 A4 纸 794px 的打印预览语义。R2 已冻结，原 480px 废弃） | **就地浮层气泡**（浮于卡片上方或侧边） |
| 内容 | 完整详情：按自定义排版结构渲染，可展示「全部字段」 | 简要预览：标题 + 副标题 + 前 N 个关键属性 |
| 层级 | 抽屉 z-index 高于卡片区；打开时卡片区可继续交互（非模态） | 浮层 z-index 高于卡片，不与抽屉冲突 |
| 关闭方式 | 抽屉右上「✕」/ Esc / 点击卡片区空白 | 鼠标移出卡片与浮层 200ms 后自动消失 |
| 互斥规则 | 抽屉打开时，未展开卡片仍可触发悬浮预览；已展开记录不重复触发 | 点击卡片 → 浮层收起，切换为抽屉 |
| 滚动行为 | 抽屉内独立滚动区 | 页面滚动时浮层收起 |

**展开动画层级（建议，供架构师参考）**
- 详情抽屉：250ms 从右滑入，伴随 15% 黑色半透明遮罩（可配为非模态无遮罩）。
- 悬浮浮层：150ms 淡入 + 轻微上移（4px），无遮罩。
- 关闭：反向动画，200ms 内完成。

### 6.4 配置态 vs 浏览态状态机

```
                点击「⚙ 配置」(有权限)
   ┌────────────┐ ───────────────────────▶ ┌────────────┐
   │  浏览态     │                          │  配置态     │
   │  Browse    │ ◀─────────────────────── │  Edit      │
   └────────────┘  保存成功 / 取消 / 放弃修改 └────────────┘
        │                                        │
        │ 卡片点击 / 悬浮                          │ 卡片点击禁用
        ▼                                        ▼
   ┌────────────┐                          ┌────────────┐
   │ 详情抽屉/浮层│                          │  预览区     │
   └────────────┘                          └────────────┘
```
- **权限驱动**：仅具备视图/表格编辑权限者可进入配置态；无权限用户「⚙ 配置」按钮不可见或置灰。
- 编辑态下卡片区仍可见，但交互降级为「预览」，防止误操作。

### 6.5 异常与空态

| 场景 | 表现 | 引导文案（示例） |
|---|---|---|
| 无记录（表为空） | 卡片区居中插图 + 说明 | 「当前表格暂无记录，请先在多维表格中新增数据」 |
| 无字段 / 无可配置字段 | 卡片区空态；字段池为空并提示 | 「当前视图暂无可用字段，请检查字段权限」 |
| 加载中（首屏） | 卡片骨架屏（每卡 4~5 条灰度占位条） | — |
| 加载中（滚动更多） | 底部加载指示器 + 骨架卡 | 「正在加载更多…」 |
| 加载失败 | 错误占位 + 重试按钮 | 「加载失败，请检查网络或稍后重试」 |
| 超长文本 | 标题区单行省略（…），属性区默认 2 行省略 | 悬浮 / 详情内可查看完整内容 |
| 字段被删除 | 配置中该字段自动失效，对应位置隐藏并提示 | 「字段『XXX』已被删除，请重新配置」 |
| 字段类型变更致不支持 | 该位置降级为纯文本兜底 | 「该字段类型暂不支持展示」 |
| 配置损坏 / 版本不兼容 | 回退到系统默认模板并保留原配置备份 | 「配置读取异常，已使用默认版式」 |

### 6.6 配置持久化的产品口径

| 问题 | 产品结论 |
|---|---|
| 配置存在哪里 | **视图级**存储：配置跟随该「卡片视图」实例，一处配置、全表共享（不绑定个人） |
| 谁能改 | 具备该表格 / 视图编辑权限的用户（对齐飞书多维表格原生权限） |
| 谁只能看 | 仅查看权限用户 → 只读浏览态，配置入口不可见 |
| 多人同时改 | 默认口径「最后保存者生效」；保存前提示他人已修改（如可实现）；冲突时提供「重新加载配置」入口 |
| 配置随视图复制 | 复制视图时配置是否一并复制 —— 见待确认 Q5 |
| 配置与数据关系 | 配置只存字段引用与样式，不存数据副本；字段删除后引用自动失效 |

---

## 7. 字段类型支持矩阵

| 字段类型 | 优先级 | 卡片区推荐展示 | 详情区推荐展示 |
|---|---|---|---|
| 文本 | P0 | 标题 / 副标题 / 属性（单行截断） | 完整文本 |
| 多行文本 | P0 | 属性区 2 行截断 | 完整段落 |
| 数字 | P0 | 属性区数值（可配千分位） | 数值 + 单位 |
| 货币 | P0 | 属性区带货币符号 | 同左，可右对齐 |
| 单选 | P0 | 彩色标签 | 彩色标签 |
| 多选 | P0 | 多个标签（限 N 个 +「+n」） | 全部标签 |
| 日期 | P0 | 属性区「字段名: YYYY-MM-DD」 | 含时间完整格式 |
| 复选框 | P0 | 勾 / 叉图标或「是 / 否」 | 同左 |
| 人员 | P1 | 头像 + 姓名（限 N 个 +「+n」） | 全部成员列表 |
| 附件 | P1 | 首图缩略图 + 附件数 | 缩略图网格 / 文件列表 |
| 评分 | P1 | 星级图标 | 星级 + 数值 |
| 进度 | P1 | 进度条 + 百分比 | 同左，可含说明 |
| 电话 | P1 | 属性区文本（可点击拨号） | 可点击拨号 |
| 邮箱 | P1 | 属性区文本（可点击发信） | 可点击发信 |
| 超链接 | P1 | 链接文本（可点击新窗打开） | 同左 |
| 公式 | P1 | 按计算结果类型渲染 | 同左 |
| 查找引用 | P1 | 按引用结果类型渲染（多值折叠） | 详情内可展开 |
| 创建人 / 创建时间 / 修改人 / 修改时间 | P1 | 属性区文本 / 日期 | 同左 |
| 自动编号 | P1 | 属性区文本 | 同左 |
| 关联（单向 / 双向） | P2 | 关联记录标题（限 N 个） | 关联记录列表 |
| 地理位置 | P2 | 属性区文本 / 地图缩略 | 地图或地址文本 |
| 群组 | P2 | 群名称标签 | 群名称标签 |
| 富文本（飞书文档字段） | P2 | 截断纯文本 | 提供打开原文入口 |
| 按钮 / 流程 / 引用汇总等 | 不支持 | 降级提示「该字段类型暂不支持」 | 同左 |

> 说明：P0 为 MVP 必须支持；P1 为紧随其后的重要类型；P2 为进阶；「不支持」类型须保证不崩溃并给出降级兜底。

---

## 8. 非功能性需求

| 类别 | 指标 | 目标 |
|---|---|---|
| 首屏性能 | 万行级表首次进入卡片视图可见首批卡片 | ≤ 3.0s（常规办公机型、网络良好） |
| 首屏性能（乐观） | 同上 | ≤ 1.5s |
| 滚动流畅度 | 滚动帧率 | ≥ 50 FPS；不出现明显卡顿与白屏 |
| 交互响应 | 配置修改到预览刷新 | ≤ 300ms |
| 悬浮预览触发 | Hover 延迟 | 150ms（防误触抖动） |
| 数据加载策略 | 分页 / 虚拟滚动 | 首批 N 张卡（建议 30~50）+ 滚动按需加载 |
| 兼容范围 | 客户端 | 飞书桌面客户端（Win / Mac）、飞书网页版（Chrome / Edge 新版） |
| 兼容范围 | 分辨率 | ≥1280px 宽度下卡片网格自适应列数 |
| 可访问性 | 键盘操作 | 卡片可 Tab 聚焦、Enter 打开详情、Esc 关闭 |
| 可访问性 | 对比度 | 文本与底色对比度符合 WCAG AA |
| 可访问性 | 语义与朗读 | 卡片提供合理的 aria 标签 |
| 国际化 | 语言 | 中文（简 / 繁）+ 英文，跟随飞书语言设置 |
| 稳定性 | 异常兜底 | 单字段渲染失败不影响整卡渲染；插件不因数据异常白屏 |
| 安全与权限 | 数据边界 | 仅读取当前多维表格数据；不落地数据副本；遵守用户身份权限 |
| 网络 | 域名白名单 | 如需请求外部服务，须在开发者后台配置白名单域名 |

---

## 9. 待确认问题清单与决策结论（已全部闭环 · 2026-09-20）

> 状态：**10 条全部已由用户拍板**，结论见下表。决策已固化到「第 11 章」与开发设计文档 `03-开发设计文档.md`。

| 编号 | 问题 | 需要谁拍板 | **确认结论** |
|---|---|---|---|
| Q1 | MVP 是否需要「卡片区写入 / 编辑记录」能力？ | 用户 / 产品 | ✅ **只读**。MVP 不做数据写回；权限基线用免审的 `bitable:app:readonly` |
| Q2 | 悬浮预览在 MVP 做成「简要气泡」还是后置 P1？ | 用户 | ✅ **按默认建议**：P0 做简要气泡，P1 升级为完整浮层 |
| Q3 | 详情展开形式偏好？ | 用户 | ✅ **右侧抽屉 + 文档式分页排版预览**（关键变更：详情不再是槽位表单，而是类似 Word 打印预览的分页文档，见第 11 章） |
| Q4 | 配置持久化的存储介质与容量上限？ | 架构师 / 技术 | ✅ 已由技术闭环：用 `bitable.bridge`，`viewId` 命名空间；容量官方未承诺，按 ≤64KB 预算 + 校验 + 降级 |
| Q5 | 复制视图 / 复制表格时配置是否一并复制？ | 用户 | ✅ **按默认建议**：不自动跟随（技术已确认无法跟随，复制生成新 viewId）；首开时提示从模板重配 |
| Q6 | 条件高亮是否需要多条件 AND / OR 复杂表达式？ | 用户 | ✅ **按默认建议**：P1 支持单层 AND/OR，不做嵌套表达式树 |
| Q7 | 移动端是否在本次交付范围？ | 用户 | ✅ **本期不支持移动端**，明确排除到 P2 |
| Q8 | 插件运行时是否存在网络请求需求？ | 用户 / 架构师 | ✅ 已由技术闭环：纯前端、零外部请求，**无需白名单、无需自建服务器** |
| Q9 | 是否需要与原生「筛选 / 排序 / 分组」对齐？ | 用户 / 技术 | ✅ **对齐原生筛选**（含排序）。基线靠传 `viewId` 自动遵循；分组无读接口，前端自行分组 |
| Q10 | 国际化范围？ | 用户 | ✅ **按默认建议**：MVP 仅简体中文，架构预留 i18n |

---

## 10. 后续可演进方向

1. **从只读走向可操作**：卡片内支持快速编辑关键字段、状态流转按钮，成为「轻量操作台」。
2. **从单表走向联动**：关联记录在卡片内可直接展开下级卡片（层级浏览）。
3. **从静态布局走向智能布局**：根据数据分布 / 使用频次自动推荐槽位配置与优先级排序。
4. **从个人配置走向团队模板**：配置模板化、团队内共享分发、一键套用组织最佳实践。
5. **多端覆盖**：移动端自适应浏览 + 精简配置能力。
6. **上架商业化路径**：满足商店上架合规要求后，从企建应用演进为可公开分发的插件（对应 P2-05）。
7. **能力外延**：卡片视图 + 分享导出 + 自动化操作联动，形成「浏览 — 分享 — 提醒」闭环。

---

## 附录 A · 产品视角的交付形态与部署期望（技术方案由架构师承接）

产品侧对"可实现方式与部署方案"的口径，供架构师据此输出技术方案：

| 维度 | 产品期望 |
|---|---|
| 交付形态 | 企业自建应用 → 多维表格插件 → 数据表视图；产出 App ID 与 BlockTypeID |
| 安装与分发 | 通过开发者后台发布企业自建应用，管理员授权后团队成员在「新建视图」中即可使用 |
| 运行形态 | 飞书客户端 / 网页内 iframe 沙箱插件，通过 `@lark-base-open/js-sdk` 读写宿主数据 |
| 权限 | 申请 `bitable:app`（编辑管理）与 `bitable:app:readonly`（查看导出），用户身份 user_access_token |
| 配置存储 | 期望配置与会话/视图绑定并持久化（具体介质与上限待 Q4 确认） |
| 网络 | 若存在外部请求，需在开发者后台配置服务器域名白名单（是否存在见 Q8） |
| 版本与升级 | 自建应用需支持版本迭代不影响既有视图配置（向后兼容） |
| 未来上架 | 若演进为商店应用，需满足上架合规审核要求（P2-05） |

---

## 11. 决策固化与需求修订（2026-09-20 · 用户已确认）

### 11.1 决策记录（D1~D10）

| # | 决策点 | 用户结论 | 对 PRD 的影响 |
|---|---|---|---|
| D1 | MVP 是否只读 | **只读** | 确认 NG1；权限基线降为 `bitable:app:readonly`（免审，可先行上线）；卡片区与详情区均无写入交互 |
| D2 | 悬浮预览 MVP 范围 | **P0 简要气泡 / P1 完整浮层** | 维持 P1-01 定位，P0 新增保底气泡（并入 P0-06） |
| D3 | 详情展开形式 | **右侧抽屉，内容为 Word 式分页文档排版预览** | ⚠️ **重大变更**：详情区由「槽位表单」升级为「文档排版引擎」，新增 P0-11~P0-15，见 11.2 |
| D4 | 复制视图时配置处理 | **不自动跟随**，首开提示从模板重配 | 确认技术结论；新增配置态提示需求（并入 P0-05） |
| D5 | 条件高亮表达式深度 | 单层 AND/OR，不做嵌套 | 维持 P1-05 边界 |
| D6 | 移动端 | **本期不支持** | 确认 NG4；兼容范围收敛为飞书桌面客户端 + 网页版 |
| D7 | 原生筛选/排序/分组对齐 | **对齐原生筛选**（含排序） | P1-08 升级为明确需求：插件视图须与原生筛选/排序结果完全一致；分组本期不做 |
| D8 | 国际化 | 仅简体中文，架构预留 | 维持 P2-06 |
| D9 | 配置容量与超限行为 | ≤64KB + 校验 + 超限提示 | 并入 P0-05 与异常态 P0-10 |
| D10 | 环境隔离 | **全部使用企业自建应用**（参考飞书视图插件部署方式） | dev / staging / prod 各一套自建应用，见技术方案 13.7 |

### 11.2 D3 详情区需求修订（核心变更）

> **变更动机（用户原话）**：「排版就是为了这个方式」——自定义排版的最终目的是产出一份**可阅读、可打印的文档**，而不只是一个信息卡片。

**修订前**：详情 = 卡片槽位结构的延伸，字段以「字段名: 值」堆叠，属**表单式**展示。
**修订后**：详情 = 一条记录渲染成**一份 A4 纸张样式的分页文档**，具备页边距、页眉页脚、页码、段落层级、键值网格、图片块、分隔线、强制分页等**文档级排版能力**，屏幕预览效果与打印/导出 PDF 一致。

**新增 P0 需求**

| 编号 | 需求 | 描述 | 验收要点 |
|---|---|---|---|
| **P0-11** | 文档式详情渲染 | 详情在右侧抽屉内以「纸页」形式渲染，支持 A4 等纸张、页边距、页眉页脚、页码 | 视觉与打印预览一致；页面尺寸/边距可配 |
| **P0-12** | 文档区块排版编辑器 | 提供区块库（标题/段落/键值网格/字段清单/标签行/图片/表格/分隔线/间距/强制分页/页脚元信息），可拖拽排序、删除、配置属性 | 区块可增删改排序；落位反馈明确；实时预览 |
| **P0-13** | 分页引擎 | 按纸张内容高度自动分页；不可切分区块整体换页；可切分区块（长段落/长表格）跨页续排 | 长内容正确分页，无内容截断/重叠/丢字 |
| **P0-14** | 打印与导出 PDF | 屏幕分页结果可直接打印/导出 PDF，保真且无二次分页错乱 | 浏览器打印对话框输出与屏幕预览一致 |
| **P0-15** | 详情预览控制 | 抽屉宽度可调 / 全屏、缩放（50%~150%）、页导航、「适应宽度」 | 缩放与翻页交互流畅；缩放不引起文字重排错乱 |

**修订后的 P0-06「卡片展开详情」验收标准（替换原 AC3）**
- AC3（新）：详情按**文档模板**渲染，支持分页、页眉页脚与页码；文档模板与卡片排版**分别配置、互不影响**。
- AC5（新）：屏幕预览与打印输出**版式一致**（同一分页结果）。

### 11.3 需求池变更总览

| 变更类型 | 条目 | 说明 |
|---|---|---|
| 新增 | P0-11 ~ P0-15 | 文档式详情渲染、区块编辑器、分页引擎、打印导出、预览控制 |
| 修订 | P0-06（AC3/AC5） | 详情渲染目标由槽位改为文档模板 |
| 修订 | P0-05 | 配置模型新增 `doc`（文档模板）分支；新增「复制视图首开提示」 |
| 修订 | P0-08 | 配置态新增「文档排版」编辑模式（双模式编辑器） |
| 修订 | P0-10 | 异常态新增：分页失败、字体缺失、图片加载失败 |
| 升级 | P1-08 | 「对齐原生筛选」确认为明确需求（含排序） |
| 收敛 | NG4 | 移动端本期明确不支持 |
| 收敛 | P1-07 | 原「详情区字段扩展」被文档模板区块模型覆盖，合并进 P0-12 |

> 详细技术设计见 `03-开发设计文档.md`。

---

## 附录 B · 详情区形态对照（修订前后）

| 维度 | 修订前（槽位表单） | 修订后（文档式分页预览） |
|---|---|---|
| 基本单元 | 槽位（标题/副标题/属性/底部） | 区块（标题/段落/键值网格/字段清单/图片/表格/分隔线/分页符…） |
| 排版目标 | 紧凑信息展示 | 纸张文档阅读（可打印、可导出） |
| 分页 | 无 | 有（A4 纸页、页边距、页眉页脚、页码） |
| 编辑方式 | 拖字段进槽位 | 拖区块进文档流 + 配置区块属性 |
| 与卡片排版的耦合 | 同一套配置 | **解耦**：卡片排版与文档排版各自独立配置 |
| 导出能力 | 无 | 打印 / 导出 PDF |
