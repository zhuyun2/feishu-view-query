# UI 设计说明与评审手册｜飞书多维表格扩展视图插件「卡片视图」（Card View）

> 本文件是**评审用说明书**：配合高保真可点击原型 `prototype/index.html` 使用。
> 原型看「长什么样」，本文件看「该看什么、判断什么、逐条勾选通过或需修改」。
> 上游输入：`01-PRD.md`（v1.1）、`03-开发设计文档.md`（v1.0）。
> 与实现强相关的字段/接口/算法细节不在此文档展开，请见 `03-开发设计文档.md`。

---

## 1. 文档信息

| 项 | 内容 |
|---|---|
| 文档名称 | 飞书多维表格扩展视图插件「卡片视图」· UI 设计说明与评审手册 |
| 版本 | v1.0 |
| 日期 | 2026-09-20 |
| 作者 | 许清楚（产品经理） |
| 评审人 | 甲方（用户）、齐活林（交付总监）、寇豆码（工程师） |
| 状态 | **待评审** |
| 配套原型 | `feishu-card-view/prototype/index.html`（高保真可点击原型，工程师产出） |
| 原型基准视口 | 桌面 **1280×800 及以上**（移动端本期不做，D6） |
| 评审方式 | 打开原型 → 逐屏对照本文件第 5 章 → 勾选第 10 章评审清单 UI-01~UI-20 → 就第 11 章 R1~R8 拍板 |

**关联前提（已确认，本设计不改动）**：MVP **只读**（无写入交互）；数据单表万行级；详情 = 右侧抽屉 + **Word 式 A4 分页文档预览**（D3，本产品核心价值）；配置复制不跟随（D4）；对齐原生筛选/排序（D7）；移动端不做（D6）。

---

## 2. 设计目标与原则

### 2.1 一句话设计定位

> **一个"长在飞书里"的阅读器，一份"能打印"的文档。**
> 卡片墙负责"一眼扫"，文档预览负责"逐条读"，两者视觉同源、职责分离。

### 2.2 五条设计原则

#### P-1 内容优先（Content First）

| 维度 | 说明 |
|---|---|
| 意味着什么 | 界面上的像素预算优先给"数据本身"。装饰性元素（分隔线、图标底、渐变）一律克制，能不画就不画。卡片的内分隔线用 1px `#E5E6EB` 而非重边框；工具栏除当前态外不使用填充色。 |
| 不意味着什么 | ≠ 简陋。层级靠字号/字重/颜色深浅三级（`#1F2329` / `#646A73` / `#8F959E`）建立，而不是靠框线堆叠。 |
| 评审怎么看 | 任意一屏截图后遮住色块，只看灰度：如果"最重的黑"落在字段值上而不是落在按钮/标题栏上，即通过。 |

#### P-2 与宿主融合（Host Native）

| 维度 | 说明 |
|---|---|
| 意味着什么 | 插件运行在飞书宿主 iframe 内，必须让人"感觉不到插件在切换"。主色、圆角、字体、字阶、控件高度全部对齐飞书多维表格的既有观感；不做深色主题皮肤，不做右侧悬浮球。 |
| 不意味着什么 | ≠ 完全无个性。**文档画布 `#EBEDF0` + 白纸 + 纸张阴影**是本产品刻意的"印刷感"差异点，属于必要的语义区分（这是"预览一份纸"而不是"看一个面板"），予以保留。 |
| 评审怎么看 | 把原型与真实的飞书多维表格并列：主色、按钮圆角、工具栏高度、字体是否"像同一个产品出的"。 |

#### P-3 文档即产品（Document is the Deliverable）

| 维度 | 说明 |
|---|---|
| 意味着什么 | 详情不是"更长的卡片"，而是一份有页边距、页眉页脚、页码、可打印的文档。因此 S2 的所有排版决策（纸张尺寸、行高、键值网格列数、跨页表头）都按**印刷品**标准做，并有独立的"文档视觉规范"（见 §5.2.6）。 |
| 不意味着什么 | ≠ 屏幕预览和打印可以不一致。屏幕与打印必须同源同版式；也 ≠ 卡片排版与文档排版共用一套配置（两者解耦，各自配置）。 |
| 评审怎么看 | 在 S2 点「适应宽度」，再看两张 A4 纸的页边距、页眉位置、页码落位是否"像 Word 打印预览"。 |

#### P-4 状态永远可解释（No Silent Failure）

| 维度 | 说明 |
|---|---|
| 意味着什么 | 任何"看起来没反应"的时刻都必须有可见反馈：为什么空、为什么灰、为什么没保存、为什么排版变了。所有降级（存储降级、配置损坏回退、分页失败转单页）都必须**明示**并给出下一步动作。 |
| 不意味着什么 | ≠ 用弹窗刷屏。降级优先用**顶部提示条（Banner）**，仅在阻断性操作（超限保存、放弃未保存修改）时用对话框。 |
| 评审怎么看 | 逐个走查 S5 的 10 个状态，确认每一条都"既能看懂发生了什么，也知道该点哪里"。 |

#### P-5 零学习成本（Zero-Training）

| 维度 | 说明 |
|---|---|
| 意味着什么 | 浏览态**没有需要学习的操作**——滚动 = 看更多，点卡片 = 看详情，⚙ = 配置，与飞书其他视图一致。配置态用"所见即所得"的拖拽 + 实时预览，不给用户看 JSON、不给用户填表单参数再"应用"。 |
| 不意味着什么 | ≠ 功能少。复杂能力（12 类区块、条件高亮、页设置）藏在配置态的右侧属性面板，按需展开，不抢占浏览态注意力。 |
| 评审怎么看 | 假设自己从没用过这个插件：只看原型不做任何解释，能否在 10 秒内说出"点哪里能改排版"。 |

---

## 3. 视觉规范（Design Tokens）

> 本章为**唯一定义源**。原型中的任何数值若与此表冲突，以此表为准。
> token 命名采用 `语义化名称`，实现侧需落到 `styles/tokens.css` 的 CSS 变量。

### 3.1 颜色 · 品牌与中性

| Token | 值 | 用途 | 使用场景 |
|---|---|---|---|
| `color-primary` | `#3370FF` | 品牌主色 | hover 卡片边框、选中态、链接、主按钮、分页边界线、focus 环 |
| `color-primary-hover` | `#4E83FD` | 主色 hover | 主按钮 hover、图标按钮 hover 文字色 |
| `color-primary-active` | `#245BDB` | 主色 active | 主按钮按下、当前页 tab |
| `color-primary-light` | `#EFF4FF` | 主色浅底 | 选中行背景、拖拽落点高亮底、插入指示线衬底、信息提示条底 |
| `color-text-1` | `#1F2329` | 主文本 | 卡片标题、文档正文、字段值、工具栏主文字 |
| `color-text-2` | `#646A73` | 次文本 | 卡片属性区字段名、副标题、说明文字 |
| `color-text-3` | `#8F959E` | 三级文本 | 占位符、页码、时间戳、辅助说明、空态副文案 |
| `color-text-disabled` | `#BBBFC4` | 禁用文本 | 禁用按钮文字、置灰字段池项 |
| `color-border` | `#DEE0E3` | 外边框 | 卡片边框、输入框边框、抽屉分隔、下拉容器边框 |
| `color-divider` | `#E5E6EB` | 内分隔 | 卡片内分隔线、工具栏下边线、列表行分隔、表格行线 |
| `color-bg-app` | `#F5F6F7` | 应用底 | 卡片区背景、表格表头底、页面底色 |
| `color-bg-panel` | `#FFFFFF` | 面板/卡片底 | 工具栏、卡片、抽屉、配置面板、纸页 |
| `color-bg-canvas` | `#EBEDF0` | **文档画布** | **仅**文档预览/编辑的纸张外围工作区（"桌面"） |

### 3.2 颜色 · 语义

| Token | 值 | 用途 | 使用场景 | 浅底 Token / 值 |
|---|---|---|---|---|
| `color-success` | `#34C724` | 成功 | 保存成功提示、已完成状态标签、勾选图标 | `color-success-light` `#E8F8E6` |
| `color-warning` | `#FF8800` | 警告 | 存储降级提示、配置体积接近上限、字段被删除 | `color-warning-light` `#FFF4E6` |
| `color-error` | `#F54A45` | 错误 | 加载失败、条件高亮"逾期"、超限拦截 | `color-error-light` `#FEECEB` |
| `color-info` | `#3370FF` | 信息 | 复制视图首开提示、分页降级提示、提示条主图标 | `color-info-light` `#EFF4FF` |

> ⚠️ 语义色**必须同时携带文字或图标**，不得单独用颜色传达状态（见 §8 可访问性）。

### 3.3 字体与字阶

| Token | 值 | 用途 |
|---|---|---|
| `font-family` | `"PingFang SC","Hiragino Sans GB","Microsoft YaHei",system-ui` | 全局字族（含文档纸张内） |
| `font-feature-numeric` | `font-variant-numeric: tabular-nums` | **所有数字**（记录数、金额、页码、百分比、日期） |
| `font-size-11` | 11px | 极小辅助（如分页边界标注） |
| `font-size-12` | 12px | 副标题、标签、徽标、页码、辅助说明 |
| `font-size-13` | 13px | 属性区字段名/值、表格单元格、抽屉工具条 |
| `font-size-14` | 14px | **正文基准**、工具栏文字、按钮、输入框、文档正文 |
| `font-size-16` | 16px | 区块小标题、面板标题、文档 H3 |
| `font-size-20` | 20px | 抽屉主标题、空态标题 |
| `font-size-22` | 22px | 文档 H1、页面级大标题 |
| `font-size-28` | 28px | 仅用于极少数强调数字（如统计数字，本期少用） |
| `font-size-card-title` | **15px / 600** | **卡片标题专用**（介于 14 与 16 之间，保证卡片在小尺寸下的视觉权重） |
| `line-height-base` | 1.6 | 文档正文、卡片多行文本 |
| `line-height-tight` | 1.35 | 卡片标题、工具栏 |
| `line-height-doc-heading` | 1.4 | 文档 H1/H2/H3 |

**文档字阶推导（`headingScale [1.6, 1.35, 1.15]` × `baseFontSize 14`）**

| 层级 | 计算 | 取整 | 字重 | 上下间距 |
|---|---|---|---|---|
| H1 | 14 × 1.6 = 22.4 | **22px** | 600 | 上下 16px |
| H2 | 14 × 1.35 = 18.9 | **19px** | 600 | 上 20px / 下 10px |
| H3 | 14 × 1.15 = 16.1 | **16px** | 600 | 上 14px / 下 8px |
| 正文 | 14 | 14px | 400 | 段落间距 12px |

### 3.4 间距与栅格

| Token | 值 | 用途 |
|---|---|---|
| `space-1 / 2 / 3 / 4` | 4 / 8 / 12 / 16px | 组件内间距基本档（4 的倍数） |
| `space-5 / 6 / 8` | 20 / 24 / 32px | 区块间距、面板内边距 |
| `space-12` | 48px | 大区块分隔、空态上下留白 |
| `layout-toolbar-height` | **48px** | 视图工具栏、抽屉工具条、编辑器顶栏统一高度 |
| `layout-grid-padding` | **16px** | 卡片区四周内边距 |
| `layout-card-min-width` | **280px** | 卡片最小宽度 |
| `layout-card-gap` | **16px** | 卡片横向与纵向间距 |
| `layout-panel-left` | 220px | 编辑器左栏（字段池 / 区块库） |
| `layout-panel-right-card` | 300px | 卡片编辑器右栏（槽位/模板/主题/高亮） |
| `layout-panel-right-doc` | 320px | 文档编辑器右栏（区块属性/页设置/文档主题） |
| `layout-drawer-default` | **860px** | 详情抽屉默认宽度（见 R2） |
| `layout-page-gap-v` | **24px** | 纸页竖向间距 |

**卡片区列数推导**：`columns = floor((viewportW − 2×16 + 16) / (280 + 16))`

| 视口宽 | 列数 | 单卡实宽（约） |
|---|---|---|
| 1280 | 4 | 300px |
| 1440 | 4 | 340px |
| 1600 | 5 | 302px |
| 1920 | 6 | 306px |

### 3.5 圆角

| Token | 值 | 用途 |
|---|---|---|
| `radius-card` | **8px** | 卡片、面板、下拉容器、抽屉内部卡片 |
| `radius-control` | **6px** | 按钮、输入框、搜索框、分段控件、槽位容器 |
| `radius-tag` | **4px** | 标签、徽标、筛选 chip、状态标签 |
| `radius-paper` | **2px** | **纸页**（刻意最小，纸质不圆） |
| `radius-pill` | 999px | 头像、圆形图标按钮 |

### 3.6 阴影

| Token | 值 | 用途 |
|---|---|---|
| `shadow-card-rest` | `0 1px 2px rgba(31,35,41,.06)` | 卡片静置 |
| `shadow-card-hover` | `0 4px 12px rgba(31,35,41,.10)` | 卡片 hover、下拉菜单、悬浮预览气泡 |
| `shadow-drawer` | `-4px 0 16px rgba(31,35,41,.08)` | 详情抽屉（向左投影） |
| `shadow-paper` | `0 1px 3px rgba(31,35,41,.12)` | **纸页**（纸张浮于灰画布之上） |
| `shadow-popover` | `0 4px 12px rgba(31,35,41,.10)` | 同 card-hover，用于属性面板的下拉、色板 |
| `shadow-drag-ghost` | `0 8px 20px rgba(31,35,41,.16)` | 拖拽中的幽灵元素（比 hover 更"离地"） |

### 3.7 动效

| Token | 值 | 用途 |
|---|---|---|
| `motion-duration-drawer-in` | **250ms** | 详情抽屉滑入 |
| `motion-duration-drawer-out` | 200ms | 抽屉关闭 |
| `motion-duration-hover-in` | **150ms** | 悬浮预览淡入（+ 上移 4px） |
| `motion-duration-hover-out` | 200ms | 悬浮预览淡出（延迟 200ms 触发） |
| `motion-duration-press` | 100ms | 按钮按下反馈 |
| `motion-duration-banner` | 200ms | 提示条出现/收起 |
| `motion-duration-focus` | 120ms | focus 环出现 |
| `motion-duration-skeleton` | 1400ms | 骨架屏微光循环 |
| `motion-preview-budget` | **≤300ms** | 配置改动 → 预览刷新的**上限预算**（不是动画，是性能红线） |
| `motion-ease-standard` | `cubic-bezier(.25,.1,.25,1)` | 通用 |
| `motion-ease-out` | `cubic-bezier(0,.0,.2,1)` | 进入类动画（抽屉、气泡） |
| `motion-ease-in` | `cubic-bezier(.4,0,1,1)` | 退出类动画 |

> **动效克制原则**：除抽屉滑入、气泡淡入、按钮/卡片 hover 三类外，一律不加过渡动画。列表项不做逐个入场动画。

### 3.8 纸张与文档度量

| Token | 值 | 用途 |
|---|---|---|
| `paper-a4-portrait` | **794 × 1123 px**（@96dpi） | 默认纸张（纵向） |
| `paper-a4-landscape` | 1123 × 794 px | 横向 |
| `paper-a5-portrait` | 559 × 794 px | 备选 |
| `paper-letter-portrait` | 816 × 1056 px | 备选 |
| `mm-to-px` | 3.7795（96 ÷ 25.4） | 页边距 UI 以 mm 呈现、内部以 px 计算 |
| `paper-margin-default` | 上下 20mm / 左右 18mm ≈ 76 / 68 px | 默认页边距 |
| `doc-zoom-range` | 50% ~ 150%，步进 25% | 缩放预设 `[0.5, 0.75, 1, 1.25, 1.5]` |
| `doc-fit-to-width` | 默认开启 | 纸张按"适应宽度"计算初始缩放 |
| `doc-header-font` | 12px / `color-text-3` / 下边距 8px | 页眉 |
| `doc-footer-font` | 12px / `color-text-3` / 上边距 8px | 页脚 |
| `doc-page-number` | 12px / `color-text-3` / `tabular-nums` | 页码（`n/total`） |

---

## 4. 信息架构与屏幕清单

### 4.1 屏幕地图

```
                        ┌──────────────────────────────┐
                        │  S1 浏览态 · 卡片墙（主入口）  │
                        └──────────────┬───────────────┘
                                       │
          ┌────────────────────────────┼────────────────────────────┐
          │ 点卡片                      │ 点 ⚙（有权限）              │ 鼠标悬停
          ▼                            ▼                            ▼
 ┌──────────────────┐      ┌────────────────────────┐     ┌─────────────────┐
 │ S2 详情抽屉        │      │ S3 配置态·卡片排版编辑器 │     │ 悬浮预览气泡     │
 │ A4 文档分页预览 ⭐  │      │（沉浸式三栏）           │     │（P0 简要气泡）    │
 └──────────────────┘      └───────────┬────────────┘     └─────────────────┘
                                       │ 顶栏模式切换
                                       ▼
                           ┌────────────────────────┐
                           │ S4 配置态·文档排版编辑器 │
                           │（沉浸式三栏）            │
                           └────────────────────────┘

 横切：S5 状态集（10 个异常/空态小场景） · S6 设计规范页（本文件的视觉化样本）
 串联：交互演示（主流程分步：卡片墙 → 悬浮 → 打开文档 → 翻页缩放 → 进配置 → 拖拽 → 保存）
```

### 4.2 屏幕清单

| 屏 | 名称 | 目的（为什么存在） | 关键区域 | 主要操作 | 关联需求 |
|---|---|---|---|---|---|
| **S1** | 浏览态 · 卡片墙 | 主入口。验证"一屏看清关键信息"这一核心价值 | ① 工具栏（48px）② 卡片墙（四槽位卡片 + hover 态卡 + 骨架卡） | 滚动、搜索、筛选、点卡片、点 ⚙ | P0-01 / P0-02 / P0-09 / P1-01 / P1-08 |
| **S2** | 详情抽屉 · A4 文档分页预览 ⭐ | **本产品核心**。验证"记录 → 可打印文档"的价值成立 | ① 工具条（48px）② 吸顶页码指示器（32px）③ 灰画布 + 两张 A4 纸 ④ 可拖拽调宽把手 | 翻页、缩放、适应宽度、全屏、打印、关闭 | **P0-06 / P0-11 / P0-13 / P0-14 / P0-15** |
| **S3** | 配置态 · 卡片排版编辑器 | 验证"零门槛自定义卡片版式" | 左 220px 字段池 / 中 预览卡 / 右 300px 槽位+模板+主题+密度+高亮；含"拖拽中"状态 | 拖字段入槽位、排序、移除、换模板、改密度、设高亮 | P0-03 / P0-04 / P0-08 / P1-02~P1-05 |
| **S4** | 配置态 · 文档排版编辑器 | 验证"记录详情可被排成一份正式文档" | 左 220px 区块库（5 组 12 类）/ 中 A4 编辑画布 + 分页标尺 / 右 320px 区块属性 + 页设置 + 文档主题 | 拖区块入文档流、排序、删块、改区块属性、改纸张/边距/页眉页脚/页码 | **P0-12 / P0-08 / P0-04** |
| **S5** | 状态集（10 个小场景） | 验证"状态永远可解释"：异常不白屏、降级有交代 | 10 个并排小场景卡 | 逐个查看 | P0-10 / D4 / D9 |
| **S6** | 设计规范页 | 把第 3 章 token 视觉化，供走查比对 | 色板、字阶、间距与圆角、阴影、组件小样 | 目视比对 | 全局视觉基线 |
| **D** | 交互演示（主流程串联） | 让评审者在 60 秒内跑通端到端主路径 | 分步串联 S1→S2→S3→S4 | 逐步点击 | 全量主流程 |

### 4.3 状态归属

| 层 | 状态 | 承载屏 |
|---|---|---|
| 全局 | Boot / Loading / Empty / Error | S1（S5 复现） |
| 浏览 | Browse / HoverPreview | S1 |
| 详情 | DocOpen / DocPaginating / Print | S2（S5 复现降级态） |
| 配置 | Edit / EditCard / EditDoc / Conflict | S3、S4 |
| 降级 | 存储降级 / 配置损坏 / 体积超限 / 复制视图首开 | S5 + Banner（S1/S3/S4 顶部） |

---

## 5. 逐屏设计说明

> 每屏结构：**布局结构图（ASCII 线框）→ 元素清单与尺寸 → 交互行为 → 边界与异常 → 评审关注点**
> ASCII 线框为"不看原型也能理解布局"的最小充分描述，示意的列数以 1280px 视口为准。

---

### 5.1 S1 浏览态 · 卡片墙

#### 5.1.1 布局结构图

```
┌───────────────────────────────────────────────────────────────────────────────────────────┐
│ 视口 1280×800                                                                              │
├───────────────────────────────────────────────────────────────────────────────────────────┤
│ ① 工具栏 Toolbar   h=48  bg=#FFFFFF  border-bottom:1px #E5E6EB  padding:0 16px              │
│ ┌───────────────────────────────────────────────────────────────────────────────────────┐ │
│ │ [ 客户卡片视图 ▾ ]  共 1,248 条  [ 🔍 搜索记录            ] [筛选 ▾] [排序 ▾] │ ⟳  ⚙  ⋯ │ │
│ └───────────────────────────────────────────────────────────────────────────────────────┘ │
│   ↑视图名下拉(400px上限) ↑记录数(13px,tab) ↑搜索框(240px)  ↑筛选chip   ↑图标按钮(32×32)      │
├───────────────────────────────────────────────────────────────────────────────────────────┤
│ ② 卡片区 Card Grid   bg=#F5F6F7  padding=16  gap=16  → 1280 视口自动 4 列，单卡约 300px     │
│                                                                                            │
│  ┌────────────────────┐ ┏━━━━━━━━━━━━━━━━━━━━┓ ┌────────────────────┐ ┌────────────────────┐ │
│  │ 客户名称            │ ┃ 客户名称            ┃ │ …                  │ │ …                  │ │
│  │ 制造业 · 张伟        │ ┃ 制造业 · 张伟        ┃ │                    │ │                    │ │
│  │ ────────────────── │ ┃ ────────────────── ┃ │                    │ │                    │ │
│  │ 合同金额      ¥ …  │ ┃ 合同金额      ¥ …  ┃ │                    │ │                    │ │
│  │ 最近跟进      …    │ ┃ 最近跟进      …    ┃ │                    │ │                    │ │
│  │ ────────────────── │ ┃ ────────────────── ┃ │                    │ │                    │ │
│  │ ● 进行中            │ ┃ ● 进行中            ┃ │                    │ │                    │ │
│  └────────────────────┘ ┗━━━━━━━━━━━━━━━━━━━━┛ └────────────────────┘ └────────────────────┘ │
│                              ↑ 第 2 张为 hover 态卡：                                      │
│                                边框 1px #3370FF + shadow-card-hover                        │
│                                                                                            │
│  ┌ ─ ─ ─ 悬浮预览气泡（浮于 hover 卡上方 8px，宽 = 卡片宽 + 32px） ─ ─ ─ ┐                   │
│  │  客户名称（14/600）                                                  │                   │
│  │  制造业 · 张伟（12/#646A73）                                          │                   │
│  │  ────────────────                                                    │                   │
│  │  合同金额  ¥ 1,280,000                                               │                   │
│  │  最近跟进  2026-09-18                                                │                   │
│  │  负责人    张伟                                                      │                   │
│  │  ────────────────                                                    │                   │
│  │  点击卡片查看完整详情 ›（12/#3370FF）                                 │                   │
│  └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘                   │
│                                                                                            │
│  ┌────────────────────┐ ┌────────────────────┐                                             │
│  │ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓     │ │ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓     │  ← 底部骨架卡（增量加载中）                  │
│  │ ▓▓▓▓▓▓▓▓▓▓         │ │ ▓▓▓▓▓▓▓▓▓▓         │     bg=#F5F6F7 微光 1.4s 循环               │
│  └────────────────────┘ └────────────────────┘                                             │
└───────────────────────────────────────────────────────────────────────────────────────────┘
```

#### 5.1.2 元素清单与尺寸

| # | 元素 | 尺寸 / 样式 | 说明 |
|---|---|---|---|
| 1 | 视图名下拉 | 高度 32px，`radius-control`，文字 14/500 `text-1` | 展示当前视图名，点击展开视图列表（对齐原生） |
| 2 | 记录数 | 13px `tabular-nums` `text-3` | 文案 `共 1,248 条`；筛选生效时显示 `共 1,248 条（已筛选 236 条）` |
| 3 | 搜索框 | 宽 240px，高 32px，`radius-control`，左内嵌放大镜 16px | 空态 placeholder `搜索记录`；对齐原生筛选（P1-08） |
| 4 | 筛选 chip | 高 28px，`radius-tag`，文字 13px | 未筛选=描边 `#DEE0E3`；已筛选=浅底 `#EFF4FF` + 主色文字 + 值 |
| 5 | 排序 / 刷新 / ⚙ / ⋯ | 图标按钮 32×32，图标 16px，hover 底 `#F5F6F7` | ⚙ = 进入配置态；无编辑权限时不渲染 ⚙ |
| 6 | 卡片 | 最小宽 280px，`radius-card`，`shadow-card-rest`，padding 12/14 | 视觉高度由内容决定；`maxCardHeight` 由密度配置控制 |
| 7 | 标题区 | 15px/600 `text-1`，`line-height 1.35`，单行省略 | 空槽位不占位（`collapsibleWhenEmpty`） |
| 8 | 副标题区 | 12px `text-2`，多字段以 ` · ` 相连，单行省略 | 默认方向 row |
| 9 | 内分隔线 | 1px `color-divider`，上下 margin 10px | **仅当标题/副标题区有内容且属性区非空时渲染** |
| 10 | 属性区 | 「字段名 13px `text-2`」左对齐 +「值 13px `text-1`」右对齐，行高 24px | 两列栅格（`label` 自适应 / `value` 右对齐 `tabular-nums`） |
| 11 | 底部区 | 贴卡底，标签高 20px/`radius-tag`/12px | 支持色块标签；无内容时整区收合 |
| 12 | 骨架卡 | 每卡 4 条灰度条，高 12px，圆角 4px，间距 10px | 仅在增量加载区出现 |
| 13 | 悬浮气泡 | 宽 = 卡宽 + 32px（上下左右各 16px 内边距），`radius-card`，`shadow-card-hover` | 见 §5.1.4 |

#### 5.1.3 交互行为

| 行为 | 触发 | 表现 |
|---|---|---|
| 滚动加载 | 距底部 < 1 屏 | 底部插入骨架卡 → 数据到达后替换为真卡，无跳变（骨架高度 = 卡高估算值） |
| 卡片 hover | 鼠标进入 | 150ms 内：边框转 `#3370FF`（1px）+ 阴影升到 `shadow-card-hover`；**卡片自身不位移**（避免抖动） |
| 悬浮预览 | hover ≥ **150ms** | 300ms 淡入（上移 4px）显示气泡；鼠标离开卡片与气泡 > 200ms 后淡出 |
| 点击卡片 | 单击卡片非交互区 | 打开 S2 详情抽屉（250ms 从右滑入），焦点移入抽屉 |
| 点击卡内交互元素 | 点击标签/链接/头像 | 不触发打开详情（事件阻止冒泡） |
| 键盘操作 | Tab 聚焦卡片 → Enter | 等价于点击卡片，打开 S2；`Esc` 关闭 |
| 刷新 | 点 ⟳ | 保留滚动位置的原地刷新，数据区短暂半透明（不整屏骨架） |

#### 5.1.4 悬浮气泡 vs 详情抽屉（P0 边界）

| 维度 | 悬浮气泡（P0 保底） | 详情抽屉（P0） |
|---|---|---|
| 内容 | 标题 + 副标题 + 前 **3** 个属性 + 「点击卡片查看完整详情 ›」 | 完整 A4 文档（见 S2） |
| 尺寸 | 宽 = 卡宽 + 32px；高度自适应，最大 260px | 860px × 100% |
| 定位 | 优先卡片上方 8px；上方空间不足则置于下方 | 右侧全高 |
| 是否阻挡卡片 | 不阻挡（`pointer-events` 在气泡内，移入气泡不关闭） | 遮挡右侧卡片区（见 R2） |

#### 5.1.5 边界与异常

| 场景 | 表现 |
|---|---|
| 空表 | 卡片区居中空态（见 S5-1） |
| 无可用字段 | 卡片区空态 + 字段池空态（见 S5-2） |
| 首屏加载中 | 首屏渲染 **8 张骨架卡**（4×2），工具栏记录数显示 `—` |
| 加载失败 | 卡片区居中错误块 + 重试按钮（见 S5-3） |
| 超长标题 | 单行省略 `…`；hover 气泡内完整显示 |
| 超长属性文本 | 属性区值默认单行省略；多行文本字段默认 2 行省略（`maxLines` 可配） |
| 条件高亮命中 | 卡片整体加 2px `color-error` 左边框条（不覆盖整卡背景，保证可读） |

#### 5.1.6 评审关注点

1. **一屏信息量**：1280px 下 4 列 × 2 行 = 8 张卡是否"够用但不拥挤"？
2. **主次是否分明**：遮住颜色只看灰度，最重的黑是否落在**字段值**而不是按钮？
3. **hover 是否克制**：卡片 hover 不位移、不放大，只变边框与阴影，是否符合预期？
4. **悬浮气泡的干扰度**：气泡会不会挡住下方卡片导致"想点却点到气泡"？（设计为移入气泡不关闭，可继续点）
5. **工具栏是否过载**：`视图名 / 记录数 / 搜索 / 筛选 / 排序 / 刷新 / 配置 / 更多` 共 8 个元素是否过长？

---

### 5.2 S2 详情抽屉 · A4 文档分页预览 ⭐（本产品核心屏）

#### 5.2.1 布局结构图

```
┌──────────────────────────────────┬──────────────────────────────────────────────────────────┐
│ 卡片区（保持可见/可交互·非模态）    │ 详情抽屉  w=860  bg=#FFFFFF  shadow-drawer                 │
│ 被抽屉遮挡约 860px                │                                                          │
│                                  │ ┃◀ 拖拽调宽把手（8px 热区，hover 变 #3370FF，可拖 560~全屏）│
│ ┌─────────┐ ┌─────────┐          │ ├──────────────────────────────────────────────────────────┤
│ │ …       │ │ …       │          │ │ ① 工具条  h=48  border-bottom:1px #E5E6EB  padding:0 16px │
│ └─────────┘ └─────────┘          │ │ 商品名称：多功能便携折叠椅   [−] 100% [+] [适应宽度]      │
│ ┌─────────┐                      │ │                              [⛶ 全屏] [🖨 打印] [✕ 关闭] │
│ │ …       │                      │ ├──────────────────────────────────────────────────────────┤
│ └─────────┘                      │ │ ② 吸顶页码指示器  h=32  bg=#FFFFFF(96% 不透明)             │
│                                  │ │   第 1 / 2 页            [☐ 显示分页边界]                  │
│                                  │ ├──────────────────────────────────────────────────────────┤
│                                  │ │ ③ 画布 Canvas  bg=#EBEDF0  padding:24 0  scroll-y         │
│                                  │ │   ┌──── 纸页 1  W794×H1123  bg#FFF radius2 shadow-paper ─┐ │
│                                  │ │   │ ← 页边距 76 / 68 →                                  │ │
│                                  │ │   │ 商品档案 · 生成于 2026-09-20        ┈┈ 页眉分隔线    │ │
│                                  │ │   │                                                     │ │
│                                  │ │   │ 多功能便携折叠椅                    ← H1 22px/600  │ │
│                                  │ │   │ ──────────────────────────────────  ← 分隔线        │ │
│                                  │ │   │                                                     │ │
│                                  │ │   │ 商品编号   SP-2026-0918  所属类目  家居户外          │ │
│                                  │ │   │ 规格尺寸   60×45×88cm    商品状态  ● 在售          │ │
│                                  │ │   │ 库存数量   1,248         上架时间  2026-08-02       │ │
│                                  │ │   │ ← 键值网格 2 列：标签 #646A73 / 值 #1F2329，行高 28 │ │
│                                  │ │   │                                                     │ │
│                                  │ │   │ 详细信息                             ← H2 19px/600 │ │
│                                  │ │   │ 商品名称    多功能便携折叠椅            ← 字段清单   │ │
│                                  │ │   │ 商品编号    SP-2026-0918               单列纵向     │ │
│                                  │ │   │ 规格尺寸    60×45×88cm                标签列宽 96  │ │
│                                  │ │   │ 库存数量    1,248                     （对齐）     │ │
│                                  │ │   │ 上架时间    2026-08-02                             │ │
│                                  │ │   │ 所属类目    家居户外                                │ │
│                                  │ │   │ 商品状态    ● 在售                                  │ │
│                                  │ │   │ 商品描述    轻量化铝合金框架…（段落，不限行）          │ │
│                                  │ │   │                                                     │ │
│                                  │ │   │ 创建人 张伟 · 创建时间 2026-08-02 · 记录ID rec…     │ │
│                                  │ │   │ ┈┈ 页脚分隔线                                        │ │
│                                  │ │   │                                          1 / 2      │ │
│                                  │ │   └─────────────────────────────────────────────────────┘ │
│                                  │ │                    ↕ 24px                                  │
│                                  │ │   ┌──── 纸页 2 ────────────────────────────────────────┐ │
│                                  │ │   │ 关联批次明细                              （续）      │ │
│                                  │ │   │ ┌──────────┬────────┬────────┬────────┐             │ │
│                                  │ │   │ │ 批次号    │ 数量   │ 入库日  │ 质检   │ ← 重复表头   │ │
│                                  │ │   │ ├──────────┼────────┼────────┼────────┤             │ │
│                                  │ │   │ │ B-0912   │ 200    │ 09-12  │ 合格   │             │ │
│                                  │ │   │ │ B-0915   │ 350    │ 09-15  │ 合格   │             │ │
│                                  │ │   │ │ …        │ …      │ …      │ …      │             │ │
│                                  │ │   │ └──────────┴────────┴────────┴────────┘             │ │
│                                  │ │   │                                                     │ │
│                                  │ │   │ ┌──────── 图片块（宽 360，居中）────────┐            │ │
│                                  │ │   │ │        [ 商品主图缩略 ]              │            │ │
│                                  │ │   │ │ 图 1 商品正面主图（caption 12/#8F959E）│           │ │
│                                  │ │   │ └──────────────────────────────────────┘            │ │
│                                  │ │   │                                          2 / 2      │ │
│                                  │ │   └─────────────────────────────────────────────────────┘ │
└──────────────────────────────────┴──────────────────────────────────────────────────────────┘
```

#### 5.2.2 元素清单与尺寸

| # | 元素 | 尺寸 / 样式 | 说明 |
|---|---|---|---|
| 1 | 抽屉容器 | 默认 **860px** 宽，100% 高，`bg-panel`，`shadow-drawer` | 右侧滑入 250ms；非模态（卡片区仍可滚动/点击其他卡片） |
| 2 | 调宽把手 | 命中区 8px × 100%，可视线 1px `color-divider` | 可拖范围 **560px ~ 视口宽**（拖满即全屏语义）；hover 时线变 `#3370FF`；双击把手 = 切"适应宽度"档 |
| 3 | 记录标题 | 14px/500 `text-1`，单行省略，最大 420px | 显示该记录"标题字段"的值 + `：` 前缀标签（如 `商品名称：…`） |
| 4 | 缩放控件 | `[−] 100% [+]`：按钮 24×24，中间百分比 13px `tabular-nums`、宽 48px | 步进 25%，范围 50%~150%；`+` 到 150% 后禁用（`text-disabled`） |
| 5 | 适应宽度 | 切换按钮，默认**开启**（文字按钮 + 激活态浅底） | 开启时 `zoom = (容器宽 − 48) / 794`，窗口 resize 重算 |
| 6 | 全屏 | 图标按钮 32×32 | 抽屉铺满视口（保留工具条）；再点还原为上次宽度 |
| 7 | 打印 | 图标按钮 32×32，tooltip `打印 / 导出 PDF` | 见 §5.2.5 打印降级链 |
| 8 | 关闭 | 图标按钮 32×32 | `Esc` / 点卡片区 / 点关闭按钮均可 |
| 9 | 页码指示器 | h=32，吸顶（`position:sticky`），底边 1px `divider` | 文案 `第 1 / 2 页`，13px `tabular-nums`；右侧「显示分页边界」复选框（**浏览态默认关**） |
| 10 | 画布 | `bg-canvas #EBEDF0`，横向 padding 24，纵向 padding 24 0 | 独立滚动容器；滚动位置映射当前页码 |
| 11 | 纸页 | **794×1123px**，`bg-panel`，`radius-paper 2px`，`shadow-paper` | 尺寸**不随 zoom 变化**（缩放用 `transform: scale`，不改布局，保证分页结果恒定） |
| 12 | 页边距 | 默认 上/下 76px、左/右 68px（≈20mm/18mm） | 由「页设置」可调；内容区 = 794−136 = **658px 宽** |
| 13 | 纸页间距 | **24px** | 相邻两纸之间 |
| 14 | 页眉 | 12px `text-3`，左对齐，内容下 8px 处 1px `divider` | 文案示例 `商品档案 · 生成于 2026-09-20`（支持字段占位符） |
| 15 | 页脚 | 12px `text-3`，内容上 8px 处 1px `divider` | 元信息：`创建人 · 创建时间 · 记录ID`（`metaFooter` 区块） |
| 16 | 页码 | 12px `text-3` `tabular-nums`，右对齐（默认） | 格式 `n/total`；与页脚同一基线区 |
| 17 | 分页边界 | 虚线 1px `#3370FF`（50% 透明）+ 左侧标注「页内容区底」11px `#8F959E` | **仅编辑态默认显示**；浏览态由指示器开关控制 |

#### 5.2.3 交互行为

| 行为 | 触发 | 表现 |
|---|---|---|
| 打开 | 点卡片 | 250ms 滑入；**先渲染纸页骨架**（灰色纸页轮廓 + 3 条占位），分页完成后替换真内容；卡片焦点内存，关闭后焦点归位 |
| 翻页 | 滚动 / 点页码指示器 / `PgUp/PgDn` | 页级虚拟化（只渲染 `currentPage ± 2`），惰性补算分页；翻页 ≤50ms |
| 缩放 | `−/+` 或 `Ctrl/Cmd + 滚轮` | 仅改 `transform: scale` + 更新容器总高；≤100ms；**不触发重新分页**（保证 zoom 50%/150% 分页结果一致） |
| 适应宽度 | 点按钮 / 双击把手 / 窗口 resize | 重算 zoom 使 794px 纸宽刚好填满容器 |
| 全屏 | 点 ⛶ | 抽屉铺满；`Esc` 先退全屏、再按才关抽屉（两级 Esc） |
| 调宽 | 拖把手 | 实时跟随（拖拽中不重排分页，只改容器宽）；松手后按需重算"适应宽度" |
| 切换记录 | 抽屉打开时点另一张卡片 | 内容原地替换（骨架 200ms 过渡），**抽屉不关闭**，保持宽度与缩放 |
| 打印 | 点 🖨 | 全量补算分页 → zoom 临时复位 1 → 隐藏宿主与插件 UI → `window.print()` → `afterprint` 恢复 |
| 关闭 | `Esc` / ✕ / 点卡片区空白 | 200ms 反向滑出；焦点回到原卡片 |

#### 5.2.4 边界与异常

| 场景 | 表现 |
|---|---|
| 分页计算中 | 纸页骨架（灰纸轮廓），指示器显示 `正在分页…` |
| 分页失败 | 顶部提示条 `分页失败，已切换为单页长文档`（info 底），画布改为连续流式渲染（无纸页边界），仍可阅读（见 S5-9） |
| 字体未就绪 | 不阻塞：先用当前字体测量并渲染，字体就绪后**自动重排**（≤3s 超时） |
| 图片未加载 | 图片位显示占位框 + 图片图标；打印前等待加载（超时 5s 跳过并保留占位） |
| 记录内容为空（全部区块 `hideWhenEmpty` 命中） | 纸页中央显示 `该记录无可展示内容` 空态（不改用整屏空态） |
| 超长文档（>200 区块 / >50 页） | 顶部提示 `文档较长，已切换为精简渲染`，并关闭分页边界提示 |
| 无配置（复制视图首次打开） | 使用默认 A4 模板渲染 + 顶部提示条（见 S5-6） |
| 打印不可用 | 降级链：新窗口打印 → 提示 `请使用 Ctrl+P` + 打印友好视图 → 导出 HTML |

#### 5.2.5 打印态（视觉口径）

| 项 | 屏幕态 | 打印态 |
|---|---|---|
| 画布背景 | `#EBEDF0` 灰 | 无（白） |
| 纸页阴影 / 圆角 | `shadow-paper` / 2px | 无 / 0 |
| 纸页间距 | 24px | 0（`break-after: page` 精确分页） |
| 缩放 | 用户 zoom | **强制 1** |
| 宿主与插件 UI | 可见 | 全部 `display:none` |
| 链接 | 蓝色可点 | 保留链接样式，输出为文本+链接 |
| 页码 | 屏幕渲染 | 同屏幕（因为是同一 DOM） |

#### 5.2.6 文档视觉规范（S2 专项）

> 这是"文档即产品"原则的落地细则，评审时请重点核对此节的每一条。

**（1）页眉 / 页脚**

| 项 | 规范 |
|---|---|
| 位置 | 位于页边距（margin）区内：页眉基线距纸顶 36px；页脚基线距纸底 36px |
| 字号 / 颜色 | 12px / `color-text-3 #8F959E` |
| 对齐 | 默认左对齐（页眉）与右对齐（页码）；可配置 left/center/right |
| 分隔线 | 页眉下、页脚上各 1px `#E5E6EB`，`showBorder` 开关控制 |
| 内容 | 支持"静态文本 + 字段占位符"混排，如 `商品档案 · 生成于 {修改时间}` |
| 首页差异 | `headerFooterScope = first` 时首页页眉页脚可单独配置（默认 all，即所有页一致） |

**（2）正文层级**

| 层级 | 字号 | 字重 | 颜色 | 间距 | 备注 |
|---|---|---|---|---|---|
| H1 | 22px | 600 | `text-1` | 上 16 / 下 16 | 一页只应出现一次；常绑定"标题字段" |
| H2 | 19px | 600 | `text-1` | 上 20 / 下 10 | 章节分隔 |
| H3 | 16px | 600 | `text-1` | 上 14 / 下 8 | 子章节 |
| 正文段落 | 14px | 400 | `text-1` | 行高 1.6，段间距 12px | 长文本**不截断**，靠分页承载 |
| 分隔线 | 1px | — | `#E5E6EB` | 上下 12px | 视觉切断，不承担语义 |
| 缩进 | `paragraphIndent` 默认 0（中文文档可设 28px = 2 字符） | | | | |

**（3）键值网格 vs 字段清单（排版差异 —— 最易混淆，务必区分）**

| 维度 | 键值网格 `keyValueGrid` | 字段清单 `fieldList` |
|---|---|---|
| 布局 | **N 列（默认 2 列）网格**，每格 "标签 + 值" | **单列纵向**列表，每行一条 |
| 适用数据 | 短值、结构化的摘要信息（编号/日期/数量/状态） | 混合长短值、需要完整展示的详情 |
| 标签位置 | 格内左侧，`labelWidthPx` 默认 96px | 行内左侧，标签列宽统一 96px（对齐） |
| 值对齐 | 左对齐；数值类右对齐并 `tabular-nums` | 左对齐 |
| 行高 | 28px（2 列时上下 padding 4×2） | 32px（行间 1px `#E5E6EB` 分隔可选） |
| 斑马纹 | `zebra` 可选（偶数行 `#FAFBFC`） | 无 |
| 冒号 | `showColon` 默认 **false**（网格内不需要冒号） | 默认 **false**（标签已列对齐） |
| 空值 | `hideEmptyRows` 默认 true（整格隐藏，网格自动补齐） | `hideEmptyItems` 默认 true |
| 分页行为 | **不可切分**（`breakInside: avoid`，整块换页） | **可切分**（按项边界切分，一项不被拆两半） |
| 视觉权重 | 摘要级，值 13px | 详情级，值 14px（与正文同级） |
| 默认模板位置 | 摘要区（H1 之后） | 详情区（H2「详细信息」之后） |

**（4）表格样式**

| 项 | 规范 |
|---|---|
| 表头 | 高 32px，`bg #F5F6F7`，文字 13px/600 `text-1`，底部 1px `#DEE0E3` |
| 单元格 | 高 32px，13px `text-1`，padding 横向 8px，行线 1px `#E5E6EB` |
| 列宽 | 按列定义 `widthPx`；未指定时均分内容区宽 |
| 数值列 | 右对齐 + `tabular-nums` |
| 斑马纹 | `zebra` 可选，偶数行 `#FAFBFC` |
| 跨页 | 长表格按**行**切分；每页**重复表头**；续页在表格右上角标注 `（续）`（11px `#8F959E`） |
| 分页安全 | 不出现"只有表头没有数据行"的孤页（至少携带 1 行） |

**（5）页码**

| 项 | 规范 |
|---|---|
| 位置 | 页脚区右下角（距右页边距 0） |
| 样式 | 12px `color-text-3`，`tabular-nums` |
| 格式 | 默认 `1 / 2`；可切 `1` 或 `第 1 页` |
| 屏幕额外 | 抽屉顶部另有**吸顶页码指示器**（`第 1 / 2 页`），与纸内页码同源同值 |

**（6）分页边界提示**

| 状态 | 表现 |
|---|---|
| 编辑态（S4） | **默认开启**：内容区底部一条 1px 虚线 `#3370FF`（50% 透明）+ 左侧标注 `页内容区底 · 658×971` |
| 浏览态（S2） | **默认关闭**；由页码指示器右侧「显示分页边界」复选框开启 |
| 纸页之间 | 不做"裁切线"装饰，仅靠 24px 间距区分 |

#### 5.2.7 评审关注点

1. **"这是纸不是面板"成立吗**：灰画布 + 白纸 + 纸影 + 页眉页脚页码，第一眼是否读作"打印预览"？
2. **860px 宽度是否合适**：在 1280 视口下抽屉占 67%，卡片区剩 420px（约 1 列）。是否接受？（→ R2）
3. **键值网格与字段清单的区分度**：不看标签文字，能否一眼分辨"摘要区"和"详情区"？
4. **页面信息密度**：第一页放 6 项键值 + 8 项字段清单 + 页眉页脚，是偏空还是偏挤？
5. **跨页表格是否可接受**：重复表头 + `（续）` 标注是否够清楚？
6. **缩放一致性**：把缩放从 50% 切到 150%，分页结果（页数、断点）是否完全不变？
7. **默认「适应宽度」**是否更符合直觉，还是会让人误以为"纸被压缩了"？（→ R5）

---

### 5.3 S3 配置态 · 卡片排版编辑器

#### 5.3.1 布局结构图

```
┌───────────────────────────────────────────────────────────────────────────────────────────┐
│ 顶栏 h=48  bg=#FFFFFF  border-bottom:1px #E5E6EB                                          │
│  [← 返回浏览]   配置卡片视图   ( ● 卡片排版 | 文档排版 )      [从模板重配 ▾] [取消] [保存]   │
│                  ↑标题 14/600   ↑分段控件(激活=浅底#EFF4FF+主色)   ↑次要   ↑次按钮 ↑主按钮  │
├──────────────┬────────────────────────────────────────────┬───────────────────────────────┤
│ 左栏 220px    │ 中栏（弹性，bg=#F5F6F7，padding 24）           │ 右栏 300px  bg=#FFFFFF         │
│ 字段池        │ 预览样例卡（宽度 = 真实卡片宽，最大 360）        │ border-left:1px #E5E6EB       │
│ bg=#FFFFFF    │                                            │                               │
│              │ ┌────────────────────────────────────────┐ │ ▸ 槽位配置                     │
│ [🔍 搜索字段] │ │ 标题区 ▏客户名称                        │ │  ┌─────────────────────────┐  │
│              │ │   ·已放置 1 个字段                      │ │  │ 标题区  ⋮⋮  客户名称  ✕  │  │
│ ▸ 已使用 (3)  │ │ ────────────────────────────────────── │ │  │ 副标题区 ⋮⋮ 行业 负责人 ✕│  │
│  ☰ 客户名称 ✅ │ │ 副标题区 ▏所属行业 · 负责人              │ │  │ 属性区  ⋮⋮ 金额 ✕ …     │  │
│  ☰ 合同金额 ✅ │ │ ────────────────────────────────────── │ │  │ 底部区   ⋮⋮ 状态 ✕       │  │
│              │ │ 属性区 ▏合同金额        ¥ 1,280,000     │ │  └─────────────────────────┘  │
│ ▸ 未使用 (6)  │ │        ▏最近跟进时间     2026-09-18     │ │  显示 ▾  方向 ▾  每卡最多 N 个 │
│  ☰ 所属行业   │ │        ▏负责人           张伟           │ │                               │
│  ☰ 负责人     │ │ ────────────────────────────────────── │ │ ▸ 布局模板  [紧凑][标准][大图][清单]│
│  ☰ 最近跟进   │ │ 底部区 ▏● 进行中                        │ │ ▸ 样式主题  主色/圆角/阴影/字号   │
│  ☰ 商品状态   │ └────────────────────────────────────────┘ │ ▸ 尺寸密度  [紧凑][标准][宽松]   │
│              │   ↑槽位边框：编辑态浅色虚线 1px #DEE0E3      │ ▸ 条件高亮  [+ 新增规则]        │
│ ▸ 不支持 (2)  │     槽位名标签 11px #8F959E                 │                               │
│  ⊘ 按钮字段   │                                            │                               │
│  ⊘ 流程字段   │ ┌── 拖拽中状态（叠加示意）────────────────┐ │                               │
│              │ │ 幽灵元素「商品状态」半透明跟随光标          │ │                               │
│              │ │ 属性区 高亮：bg=#EFF4FF + 边框 1px #3370FF │ │                               │
│              │ │ 属性区 ▏合同金额                          │ │                               │
│              │ │ ━━━━━━━━━  ← 2px 插入指示线 #3370FF      │ │                               │
│              │ │ 属性区 ▏最近跟进时间                      │ │                               │
│              │ └────────────────────────────────────────┘ │                               │
├──────────────┴────────────────────────────────────────────┴───────────────────────────────┤
│ 编辑态约束：卡片墙点击展开详情【禁用】；顶栏「保存」为唯一出口；有未保存修改时关闭需二次确认   │
└───────────────────────────────────────────────────────────────────────────────────────────┘
```

#### 5.3.2 元素清单与尺寸

| # | 元素 | 尺寸 / 样式 | 说明 |
|---|---|---|---|
| 1 | 顶栏 | h=48，`bg-panel`，底边 1px `divider` | 全屏沉浸，**不保留卡片墙**（见 R6） |
| 2 | 返回浏览 | 文字按钮 + 左箭头图标 | 等价"取消"；有未保存修改时二次确认 |
| 3 | 模式分段控件 | 高 32px，`radius-control`，两段（卡片排版 / 文档排版） | 激活段：底 `#EFF4FF` + 文字 `#3370FF` + 600 |
| 4 | 从模板重配 | 下拉按钮（次要），高 32px | 仅在 D4 复制视图场景高亮显示 |
| 5 | 取消 / 保存 | 取消=描边按钮；保存=主按钮 `#3370FF`；高 32px | 保存为唯一提交出口；无脏值时"保存"禁用 |
| 6 | 字段池 | 宽 220px，项高 36px，`radius-control`，行间距 4px | 三组：已使用 / 未使用 / 不支持 |
| 7 | 字段池项 | 左 `☰` 拖柄 16px，中 字段名 13px，右 已使用勾 `✓` | hover 底 `#F5F6F7`；不支持项文字 `#BBBFC4` + ⊘ 图标 + tooltip 原因 |
| 8 | 字段池搜索 | 高 32px，`radius-control` | 字段多时使用；搜索命中的"未使用"项置顶 |
| 9 | 预览样例卡 | 与真实卡片同宽（最大 360px），居中，`shadow-card-rest` | 固定用"首条有值记录"，右上角标注 `示例记录` 11px `#8F959E` |
| 10 | 槽位容器 | 编辑态：1px 虚线 `#DEE0E3`，`radius-control`，内 padding 6px；槽位名标签 11px `#8F959E` | 空槽位保留可见（否则无法落位），显示 `拖字段到此处` 12px `#BBBFC4` |
| 11 | 已放置字段项 | 高 28px，hover 显示 `⋮⋮` 拖柄 + `✕` | 拖柄 16px；`✕` 16px，hover 变 `color-error` |
| 12 | 幽灵元素 | 被拖字段的副本，`opacity .6`，`shadow-drag-ghost`，`radius-control` | 跟随光标，偏移 (12, 12) |
| 13 | 插入指示线 | **2px** `#3370FF`，宽度 = 槽位内容宽 | 落在两个字段之间显示；落在槽位空白 = 追加末尾（先高亮槽位底 `#EFF4FF`） |
| 14 | 右栏面板 | 宽 300px，分组折叠（默认展开第一组） | 组标题 13px/600，右侧 `▾` |
| 15 | 模板画廊 | 4 个缩略图 tile，宽 62px 高 44px，`radius-control` | 紧凑 / 标准 / 大图 / 清单；选中态 1px `#3370FF` + 浅底 |
| 16 | 密度分段 | 三档：紧凑 / 标准 / 宽松 | 联动改 `cardMinWidth / gap / padding / maxCardHeight` |

#### 5.3.3 交互行为

| 行为 | 触发 | 表现 |
|---|---|---|
| 进入配置态 | 点 ⚙ | 顶栏 + 三栏立即铺开（120ms 淡入）；卡片墙隐藏 |
| 拖拽开始 | 按字段池项 ≥ 200ms（长按阈值防误触） | 出现幽灵元素；四个槽位同时进入"可落位"态（虚线变 `#3370FF`，底 `#EFF4FF`） |
| 拖拽中 | 移动 | `onDragOver` 计算最近项中点 → 显示 2px 插入指示线；光标进入槽位空白 → 槽位整体高亮 |
| 放下 | 松手 | 字段插入指示位置；槽位回到常态；预览卡在 ≤300ms 内刷新；字段池该项标 `✓ 已使用` |
| 槽位内排序 | 拖 `⋮⋮` | 同槽位内实时让位 + 指示线；松手落位 |
| 移除 | 点 `✕` | 该项消失（100ms 淡出）；字段池该字段回到"未使用"；预览刷新 |
| 重复放置 | 拖同一字段到同槽位 | **允许**（PRD AC3：同一字段可放多个槽位）；同槽位内重复则在落位处提示 `该字段已在本次槽位中，将重复显示`（inline 提示，不阻塞） |
| 换模板 | 点模板 tile | 槽位配置整体替换，**保留已用字段**（尽力映射），未映射字段回池；替换前若已有自定义配置则二次确认 `套用模板将覆盖当前槽位配置，是否继续？` |
| 改密度 | 点密度段 | 中栏预览卡与右栏数值实时变化 |
| 添加高亮规则 | 点 `+` | 右栏内联展开规则表单：字段 + 运算符 + 值 + 样式（边框/文字/标签）；单层 AND/OR 切换 |
| 保存 | 点保存 | 体积校验（≤64KB）→ 写入 → 成功 toast `配置已保存，团队成员均可见`（2s 自动消失）→ 回浏览态 |
| 取消 | 点取消 / 返回浏览 | 有脏值时二次确认 `放弃未保存的修改？`（`放弃 / 继续编辑`）；确认后回滚到进入前快照 |
| 模式切换 | 点"文档排版" | 中栏切为 A4 编辑画布；右栏切为区块属性面板；**卡片草稿保留**（不丢弃、不互相污染） |

#### 5.3.4 边界与异常

| 场景 | 表现 |
|---|---|
| 无可用字段 | 字段池整块空态 `当前视图暂无可用字段，请检查字段权限`；中栏预览卡显示空槽位提示 |
| 字段被删除（配置引用了已删除字段） | 已放置项显示为 `失效字段` 12px `#F54A45` + `✕` 只能删除不能拖；右栏顶部一条 warning 提示 `配置中有 1 个字段已被删除` |
| 字段类型不支持 | 字段池"不支持"分组内；若已放置在槽位，项后缀 `⊘ 类型不支持`，预览卡该位置显示降级文案 |
| 拖到非法区域 | 幽灵元素显示"禁止"光标；松手无动作，幽灵 150ms 淡出 |
| 配置体积超限 | 点保存时拦截 → 对话框 `配置过大（68KB / 上限 64KB），请精简区块或高亮规则` + `知道了` |
| 他人已修改 | 保存时冲突 → 对话框 `该视图配置已被他人修改` + `重新加载配置 / 强制保存` |
| 无编辑权限 | ⚙ 不渲染（浏览态无编辑入口） |

#### 5.3.5 评审关注点

1. **三栏是否够用**：220 / 弹性 / 300 的分配在 1280 视口下，中栏预览卡还有约 700px，是否舒适？
2. **"沉浸式"是否可接受**：进入配置态后卡片墙消失，是否让人失去上下文？（→ R6）
3. **拖拽反馈强度**：幽灵 + 槽位高亮 + 2px 指示线三重反馈是否过强/过弱？
4. **右栏是否过挤**：300px 内塞入槽位配置 + 模板 + 主题 + 密度 + 条件高亮 5 组，折叠是否必要（目前只展开第一组）？
5. **预览卡不显示真实卡片网格**：只给一张样例卡，是否够"所见即所得"？

---

### 5.4 S4 配置态 · 文档排版编辑器

#### 5.4.1 布局结构图

```
┌───────────────────────────────────────────────────────────────────────────────────────────┐
│ 顶栏 h=48   [← 返回浏览]  配置卡片视图   ( 卡片排版 | ● 文档排版 )   [重置为默认模板] [取消] [保存] │
├──────────────┬────────────────────────────────────────────────────────────┬─────────────────┤
│ 左栏 220px    │ 中栏（bg=#EBEDF0 灰画布，padding 24，scroll）                 │ 右栏 320px       │
│ 区块库        │                                                            │ bg=#FFFFFF      │
│ bg=#FFFFFF    │  ┌── A4 编辑画布 794×1123 白纸 ────────────────────────┐    │ border-left 1px │
│              │  │ 分页标尺（顶部 1px 虚线 #3370FF）                     │    │                 │
│ ▸ 文本        │  │ ┈┈┈┈┈┈┈┈┈┈┈┈┈┈ 页内容区底 ┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈  │    │ ▸ 区块属性       │
│  ☰ 标题 H1-H3 │  │                                                     │    │  类型：键值网格   │
│  ☰ 段落       │  │  H1 商品名称（绑定字段）                              │    │  列数  (1)(2)(3)(4)│
│  ☰ 静态文本    │  │  ─────────────────────────────────                  │    │  标签列宽 96px    │
│              │  │  ▏键值网格 2 列                       ← hover 态      │    │  ☐ 显示冒号       │
│ ▸ 字段        │  │    (hover 显示 ⋮⋮ 拖柄 + ✕ + 区块类型标签)              │    │  ☐ 斑马纹         │
│  ☰ 键值网格   │  │  ▏摘要 · 商品编号 · 类目 · 规格 · 状态               │    │  ☑ 隐藏空值行     │
│  ☰ 字段清单   │  │                                                     │    │  ─────────────   │
│  ☰ 标签行     │  │  ┏━ H2 详细信息 ━━━━━━━━━━━━━━━━┓  ← 选中态        │    │  [上移] [下移] [删除]│
│              │  │  ┃ (选中：1px #3370FF + 浅底 + 类型标签)              │    │                 │
│ ▸ 媒体        │  │  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━┛                      │    │ ▸ 页设置         │
│  ☰ 图片       │  │  字段清单（8 项）                                    │    │  纸张 A4 ▾ 方向 ▾ │
│  ☰ 表格       │  │  ━━━━━━━━ ← 2px 插入指示线（拖拽中）                 │    │  页边距 上下 20mm │
│              │  │  ▏  ┈┈┈┈┈┈┈ 页内容区底（分页边界提示）┈┈┈┈┈┈┈          │    │        左右 18mm │
│ ▸ 结构        │  │                                                     │    │  ☑ 显示页眉      │
│  ☰ 分隔线     │  │  页脚：创建人 · 时间 · 记录 ID                        │    │  ☑ 显示页脚      │
│  ☰ 间距       │  │                                          1 / 2      │    │  ☑ 显示页码      │
│  ☰ 强制分页   │  └─────────────────────────────────────────────────────┘    │  页码格式 n/total│
│              │                    ↕ 24px                                     │                 │
│ ▸ 元信息      │  ┌── A4 编辑画布（第 2 页）────────────────────────────┐    │ ▸ 文档主题       │
│  ☰ 页脚元信息 │  │  …                                                   │    │  字体 PingFang ▾ │
│              │  └─────────────────────────────────────────────────────┘    │  基础字号 14px   │
│              │                                                            │  行高 1.6        │
│  ↕ 拖区块到文档 │                                                            │  标题倍率 1.6/1.35/1.15│
│              │                                                            │  区块间距 12px   │
│              │                                                            │  主色 #3370FF    │
│              │                                                            │  [恢复默认]      │
├──────────────┴────────────────────────────────────────────────────────────┴─────────────────┤
│ 底栏提示 12px #8F959E：拖拽可插入任意位置 · 点选区块在右侧改属性 · 改动 ≤300ms 反映到预览        │
└───────────────────────────────────────────────────────────────────────────────────────────┘
```

#### 5.4.2 元素清单与尺寸

| # | 元素 | 尺寸 / 样式 | 说明 |
|---|---|---|---|
| 1 | 区块库（左栏 220px） | 分 **5 组 12 类**，项高 36px | 组标题 13px/600 `text-2`；组可折叠 |
| 2 | 分组定义 | 文本(标题/段落/静态文本) · 字段(键值网格/字段清单/标签行) · 媒体(图片/表格) · 结构(分隔线/间距/强制分页) · 元信息(页脚元信息) | 与 03 文档 §5.2 一一对应 |
| 3 | 区块库项 | `☰` 16px + 中文名 13px + 右侧 tooltip 图标 | hover 底 `#F5F6F7`；拖动出 `ghost` |
| 4 | 编辑画布 | `bg-canvas #EBEDF0`，padding 24，纵向滚动 | 与 S2 浏览态**同一套渲染**，只是叠加编辑装饰 |
| 5 | 纸页 | 794×1123，`shadow-paper`，`radius-paper` | 编辑态**默认 100% 缩放**（不做适应宽度，避免拖拽坐标偏移）；缩放下限 50% |
| 6 | 分页标尺 | 1px 虚线 `#3370FF`（50% 透明）+ 左侧标签 `页内容区底 · 658×971`（11px `#8F959E`） | 编辑态默认显示 |
| 7 | 强制分页标尺 | 在含 `pageBreak` 处显示一条 1px 实线 `#3370FF` + 标签 `分页` | 拖入 `pageBreak` 时出现 |
| 8 | 间距标尺 | `spacer` 区块 hover 时显示高度标签 `12px` | 便于校准间距 |
| 9 | 区块 hover 态 | 左侧出现 `⋮⋮` 拖柄（16px，`#8F959E`）+ 右上浮出 `✕`（16px）+ 右上角区块类型标签（11px `#8F959E`） | 不改变区块本身视觉 |
| 10 | 区块选中态 | 1px `#3370FF` 描边 + 浅底 `#EFF4FF`（10% 透明度，不遮内容）+ 左上角类型标签（浅底徽标） | 选中后右栏切到该区块属性 |
| 11 | 插入指示线 | **2px** `#3370FF`，贯穿内容区宽 | 拖拽中显示 |
| 12 | 空文档提示 | 纸页中央 `从左侧区块库拖入第一个区块` 14px `#BBBFC4` + 虚线投放区 | 无区块时 |
| 13 | 右栏（320px） | 三组：区块属性（含类型名徽标）/ 页设置 / 文档主题 | 未选中区块时"区块属性"显示"未选择区块"占位 |
| 14 | 区块属性面板 | 按 `kind` 动态表单；布局：标签 13px `text-2` 左 / 控件右 | 例：键值网格 → 列数分段、标签列宽数字输入、冒号/斑马纹/隐藏空行复选框、行列表（可增删排序） |
| 15 | 页设置 | 纸张(下拉 A4/A5/Letter) · 方向(纵向/横向) · 页边距(4 个数字输入，mm) · 页眉开关+内容+对齐 · 页脚开关+内容+对齐 · 页码开关+格式 | 改纸张/边距时**画布尺寸即时变化** |
| 16 | 文档主题 | 字体(下拉) · 基础字号(数字+滑块) · 行高 · 标题倍率 3 值 · 区块间距 · 首行缩进 · 主色/文字色/弱化色/分隔色 | 改任一项 → 全文档重排（≤300ms 预算） |
| 17 | 图层操作 | 选中区块后右栏底部 [上移] [下移] [删除] | 键盘可达：`Alt+↑/↓` 移动，`Delete` 删除 |
| 18 | 底栏提示 | h=32，12px `#8F959E` | 固定一行操作提示 |

#### 5.4.3 交互行为

| 行为 | 触发 | 表现 |
|---|---|---|
| 拖入区块 | 从区块库拖到文档流 | 幽灵元素 + 2px 指示线；松手插入；新块使用 `blockDefaults` 默认配置；**拖入后自动选中**并聚焦右栏第一项 |
| 拖入空白文档 | 拖到空纸页中央 | 投放区高亮，松手插入为第 1 块 |
| 排序 | 拖区块（画布内或 `⋮⋮`） | 实时让位 + 指示线；跨页拖拽时自动滚动并可**临时暂停页虚拟化**（拖到未渲染页不失败） |
| 删除 | `✕` / 右栏 [删除] / `Delete` | 100ms 淡出；后续区块上移；重新分页 |
| 选中 | 单击区块 | 选中态出现；右栏换为该块属性；`Esc` 取消选中 |
| 改属性 | 右栏任意控件 | 画布**只重排受影响区块起**到末尾，防抖 120ms，总预算 ≤300ms |
| 改页设置 | 纸张/方向/边距 | 画布纸张尺寸与分页标尺即时变化；页数可能增加（提示 `调整后共 3 页`） |
| 改文档主题 | 字号/行高/间距/颜色 | 全文档重排；颜色只重绘不重排 |
| 重置为默认模板 | 点顶栏按钮 | 二次确认 `将丢弃当前文档排版，恢复为默认 A4 模板？` → 恢复 `defaultDocTemplate()` |
| 保存 | 点保存 | 同 S3；保存后回浏览态，卡片与文档配置一并提交 |
| 切到卡片排版 | 点分段控件 | 中栏切回预览卡，右栏切回槽位面板；**文档草稿保留** |

#### 5.4.4 边界与异常

| 场景 | 表现 |
|---|---|
| 无可用字段 | 区块库"字段"组内的块可拖入，但属性面板提示 `暂无可绑定字段`；画布中绑定块显示 `字段缺失` 占位 |
| 字段被删除 | 画布中该块左上角 warning 徽标 + 属性面板字段选择器标红 `失效` |
| 空文档 | 纸页中央空态 + 虚线投放区（见元素 12） |
| 分页失败 | 画布切为单页长文档 + 顶部提示条；编辑能力保留（仅失去分页标尺） |
| 文档过大（>200 区块） | 顶部提示 `文档较长，已切换为精简渲染`；分页边界提示自动关闭 |
| 缩放到 <100% | 拖拽落点按缩放反算（避免落位偏移）；缩放 <75% 时提示 `建议 100% 编辑以获得准确落点` |
| 页边距过大导致内容区过小 | 内容区宽 < 300px 时提示 `页边距过大，内容区仅 280px，可能影响排版` |

#### 5.4.5 评审关注点

1. **区块库分组是否好找**：5 组 12 类的中文命名是否直觉（尤其"键值网格 / 字段清单 / 标签行"三者的差别）？
2. **"选中区块 → 右侧改属性"是否够快**：相比"每块展开内联编辑"，右栏是否更清楚？
3. **编辑态不缩放（100%）** 是否比浏览态"适应宽度"更合理？
4. **页设置的信息量**：320px 内塞入纸张/方向/页边距/页眉/页脚/页码共 6 项，是否需要分组折叠？
5. **表单用 mm 还是 px 表达页边距**：目前 UI 用 mm（20mm/18mm），是否符合非设计人员的直觉？（→ 建议保留 mm，因"打印"语义）

---

### 5.5 S5 状态集（10 个小场景）

> 展示方式：10 个并排的场景卡（每个约 300×200），卡片顶部为场景名，中间为真实视觉表现，底部为触发说明。
> 完整 16 条状态规范见第 6 章，此处只做视觉样本。

| # | 场景 | 画面要点 |
|---|---|---|
| S5-1 | 无记录（空表） | 卡片区居中插图 + 主文案 `当前表格暂无记录` + 副文案 `请先在多维表格中新增数据` + 次要按钮 `刷新` |
| S5-2 | 首屏加载中 | 8 张骨架卡（4×2）+ 工具栏记录数 `—` |
| S5-3 | 加载失败 | 卡片区居中：error 图标 `#F54A45` + `加载失败，请检查网络或稍后重试` + 主按钮 `重试` |
| S5-4 | 配置损坏回退 | 顶部 info 提示条 `配置读取异常，已使用默认版式` + 右侧 `查看备份` 文字按钮；卡片区按默认模板正常渲染 |
| S5-5 | 存储降级提示 | 顶部 **常驻** warning 提示条 `配置仅保存在本地，其他成员看不到你的排版`，**无关闭按钮** |
| S5-6 | 复制视图首开提示 | 顶部 info 提示条 `本视图由其他视图复制而来，排版需重新配置` + `从模板重配` / `立即配置` 两个按钮 + `✕`（本会话不再提示） |
| S5-7 | 字段被删除 | 卡片区正常渲染，失效字段位置留空；配置态内该项标红 `失效字段`；顶部 warning 条 `1 个字段已被删除` |
| S5-8 | 字段类型不支持 | 该字段位置显示 `该字段类型暂不支持展示` 12px `#8F959E` + ⊘ 图标；卡片其余内容正常；不崩溃 |
| S5-9 | 分页失败 → 单页长文档 | 抽屉内顶部 info 条 `分页失败，已切换为单页长文档`；画布无纸页边界，内容连续流式；仍可滚动阅读 |
| S5-10 | 配置体积超限 | 保存时对话框 `配置过大（68 KB / 上限 64 KB）` + 副文案 `请精简区块或高亮规则后重试` + `知道了` |

#### 5.5.1 评审关注点

1. 10 个状态的**文案是否可以直接上线**（没有占位符、没有内部术语）？
2. 降级提示（S5-4/5/9）是否**足够显眼但不扰民**？
3. S5-5 的存储降级提示**不可关闭**是否过于强硬？
4. 异常态的插图/图标风格是否与飞书宿主一致（不自创插画风格）？

---

### 5.6 S6 设计规范页

| 区块 | 内容 |
|---|---|
| 色板 | 品牌 4 色 + 中性 6 色 + 语义 4 色（各含浅底），每色块标注 token 名与 HEX |
| 字阶 | 11/12/13/14/15/16/20/22/28 逐号显示，右侧标注用途；`15px/600` 卡片标题单独高亮 |
| 间距与圆角 | 4/8/12/16/20/24/32/48 方块可视化；圆角 8/6/4/2 四例 |
| 阴影 | 5 个阴影样例卡并排（rest/hover/drawer/paper/ghost） |
| 组件小样 | 按钮 5 态、输入框 4 态、标签 4 色、复选框、下拉、分段控件、提示条 4 色、徽标、骨架块 |
| 纸张 | A4 794×1123 等比缩略 + 页边距标注 + 页眉页脚位置标注 |

#### 5.6.1 评审关注点

1. 色板与飞书宿主是否"像一家人"（特别是主色与中性灰）？
2. 语义浅底色值（`#E8F8E6 / #FFF4E6 / #FEECEB`）是否与飞书原生标签色一致？（→ 见 D-UI-6）

---

### 5.7 交互演示（主流程串联）

| 步 | 演示动作 | 观察点 |
|---|---|---|
| 1 | 进入 S1，卡片墙渲染完成 | 首屏骨架→真卡无跳变 |
| 2 | 鼠标悬停第 2 张卡 ≥150ms | 气泡淡入（150ms）不抖动 |
| 3 | 点击该卡片 | 抽屉 250ms 滑入 + 纸页骨架→真文档 |
| 4 | 点 `−` 到 75%，再点「适应宽度」 | 分页结果不变，只有视觉缩放 |
| 5 | 滚动到第 2 页 | 吸顶页码指示器跟随更新；跨页表格带 `（续）` 表头 |
| 6 | 拖拽抽屉左边缘到全屏，再还原 | 把手 8px 热区可拖；`Esc` 两级（退全屏→关抽屉） |
| 7 | `Esc` 关闭抽屉 | 焦点回到原卡片 |
| 8 | 点 ⚙ 进入配置态（卡片排版） | 三栏铺开；卡片墙隐藏；点击卡片不展开（禁用） |
| 9 | 拖"商品状态"到属性区 | 幽灵 + 槽位高亮 + 2px 指示线；预览 ≤300ms 更新 |
| 10 | 切到"文档排版" | 草稿保留；画布为真实 A4 文档 |
| 11 | 拖"键值网格"到 H1 之后 | 插入指示线；新块自动选中；右栏改列数 2→3 |
| 12 | 点保存 | 回浏览态 + 成功 toast；回到 S1 确认卡片已变 |

---

## 6. 状态与反馈规范

> 对齐 `03-开发设计文档.md` §12，共 **15 类**；另补 1 条产品决策态（D4 复制视图首开），合计 **16 条**。
> 所有状态必须满足：**可解释（说清为什么）+ 可操作（给出下一步）+ 不阻塞阅读（除非必须阻塞）**。

| # | 状态 | 触发条件 | 视觉表现 | 文案 | 可操作项 | 是否阻塞 |
|---|---|---|---|---|---|---|
| 1 | 无记录（空表） | 视图无任何记录 | 卡片区居中插图（灰调线性图标 64px）+ 主副文案 | 主：`当前表格暂无记录`<br>副：`请先在多维表格中新增数据` | `刷新`（次要按钮） | 否（空态本身即终态） |
| 2 | 无字段 / 无可配置字段 | 字段元数据为空或无可见字段 | 卡片区居中空态；配置态字段池内空态 | 卡片区：`当前视图暂无可用字段`<br>字段池：`当前视图暂无可用字段，请检查字段权限` | `检查字段权限`（文字链，跳原生） | 否 |
| 3 | 首屏加载中 | 首次取数未返回 | **8 张骨架卡**（4×2）+ 工具栏记录数 `—` | — | 无 | 否 |
| 4 | 增量加载中 | 滚动触底加载更多 | 底部 2 张骨架卡 + 居中 `正在加载更多…`（12px `#8F959E`） | `正在加载更多…` | 无 | 否 |
| 5 | 加载失败 | 取数抛错 / 超时 | 卡片区居中：error 图标 + 文案 + 主按钮 | `加载失败，请检查网络或稍后重试` | `重试` | 是（该区域不可用） |
| 6 | 超长文本 | 字段值超出容器 | 卡片区单行/2 行省略 `…`；**文档区不截断**，由分页承载 | —（无文案） | 悬浮气泡/抽屉内可看全文；`查看完整内容` tooltip | 否 |
| 7 | 字段被删除 | 配置引用的 fieldId 不在元数据中 | 卡片区该位置隐藏（不占位）；配置态该项标红；顶部 warning 条 | `字段「XXX」已被删除，请重新配置` | `前往配置`（文字按钮） | 否 |
| 8 | 字段类型变更致不支持 | 字段类型不在支持矩阵 | 该位置显示降级文案 + ⊘ 图标，卡片其余正常 | `该字段类型暂不支持展示` | 配置态内可汇总查看 | 否 |
| 9 | 配置损坏 / 版本不兼容 | checksum 失败 / schemaVersion > 当前 | 顶部 info 提示条 + 按默认模板渲染；更高版本时进入**只读模式** | 损坏：`配置读取异常，已使用默认版式`<br>更高版本：`配置由更高版本创建，当前为只读` | `查看备份`（损坏）／只读时禁用保存 | 否（回退方案可用）／只读模式下**禁止保存** |
| 10 | ⭐ 分页失败 / 测量异常 | `paginate()` 抛错或高度测量异常 | 抽屉顶部 info 条；画布无纸页边界，改为连续流式渲染 | `分页失败，已切换为单页长文档` | `重试分页`（文字按钮） | 否（内容仍可读） |
| 11 | ⭐ 字体未就绪 | 分页前 `document.fonts.ready` 未完成 | 不显示提示；先用当前字体渲染，字体就绪后**自动重排**（≤3s 超时后重排） | —（静默，避免干扰） | 无 | 否 |
| 12 | ⭐ 图片加载失败 / 未完成 | 附件 URL 失效或超时 | 图片位显示灰底占位框 + 图片图标 + 尺寸一致（不留空白塌陷） | `图片加载失败`（12px `#8F959E`）；打印时若未完成 → `图片未加载，已跳过` | `重新加载`（占位框内文字按钮） | 否 |
| 13 | ⭐ 打印不可用 | `window.print()` 被沙箱限制 | 依次降级：① 新窗口打印 ② 提示条 + 打印友好视图 ③ 导出 HTML | ② `当前环境不支持直接打印，请使用 Ctrl+P` ／ ③ `已提供 HTML 文件供离线打印` | `打开打印友好视图` / `导出 HTML` | 是（该功能不可用） |
| 14 | ⭐ 配置体积超限 | 保存前校验 > 64KB | 对话框（居中，遮罩 15% 黑） | 标题：`配置过大`<br>正文：`当前 68 KB，上限 64 KB。请精简区块或高亮规则后重试` | `知道了` | 是（拦截保存） |
| 15 | ⭐ 存储降级 | `bitable.bridge` 不可用，回落 localStorage | 顶部 **常驻** warning 提示条（不可关闭） | `配置仅保存在本地，其他成员看不到你的排版` | `了解详情`（tooltip 展开原因） | 否 |
| 16 | D4 复制视图首开提示 | `provisionedFromTemplate === true` 首次打开 | 顶部 info 提示条 | `本视图由其他视图复制而来，排版需重新配置` | `从模板重配`（主）/ `立即配置`（次）/ `✕`（本会话不再提示） | 否 |

### 6.1 提示条（Banner）统一规范

| 项 | 规范 |
|---|---|
| 位置 | 工具栏下方，全宽，h=40 |
| 结构 | 图标 16px + 文案 13px `text-1` + 操作区（文字按钮，右对齐）+ 可选 `✕`（16px） |
| 配色 | info：底 `#EFF4FF` / 图标 `#3370FF`；warning：底 `#FFF4E6` / 图标 `#FF8800`；error：底 `#FEECEB` / 图标 `#F54A45`；success：底 `#E8F8E6` / 图标 `#34C724` |
| 边框 | 底部 1px 对应语义色的 20% 透明度 |
| 多条并存 | 最多同时 2 条；优先级 error > warning > info；超出的收进 `还有 1 条提示 ▾` |
| 动效 | 200ms 高度展开 + 淡入 |
| 不可关闭场景 | 仅"存储降级"（S5-5） |

### 6.2 反馈时长规范

| 操作 | 反馈方式 | 时长 |
|---|---|---|
| 保存成功 | 顶部 toast（success） | 2s 自动消失 |
| 复制成功 / 导出发起 | toast（info） | 2s |
| 二次确认（放弃修改 / 套用模板 / 重置模板） | 对话框 | 需用户操作 |
| 拖拽落位 | 无 toast，靠视觉反馈 | — |
| 预览刷新 | 无提示，静默 | ≤300ms |

---

## 7. 组件清单

> 状态列含义：default / hover / active / focus / disabled（未涉及的态写 `—`）。
> 尺寸除特别说明外，均指**桌面端**；最小点击热区 **32×32px**（见 §8）。

| # | 组件 | 变体与状态 | 尺寸 | 使用场景 |
|---|---|---|---|---|
| 1 | **按钮 Button** | 变体：主（填充 `#3370FF`）/ 次（描边 `#DEE0E3`）/ 文字（无底）/ 危险（`#F54A45`）<br>状态：default / hover（`#4E83FD` 或底 `#F5F6F7`）/ active（`#245BDB` 或底 `#E5E6EB`）/ focus（2px `#3370FF` 外环，offset 1px）/ disabled（底 `#F5F6F7`、文字 `#BBBFC4`、`cursor:not-allowed`） | 高 32px（主/次），文字按钮高 28px；`radius-control`；内边距 12px；图标按钮 32×32 | 保存/取消/重试/删除/文字链 |
| 2 | **输入框 Input** | 状态：default（1px `#DEE0E3`）/ hover（1px `#BBBFC4`）/ focus（1px `#3370FF` + 外环 `#EFF4FF` 2px）/ disabled（底 `#F5F6F7`）/ error（1px `#F54A45`）<br>`radius-control`，高 32px，内边距 0 10px，文字 14px | 高 32px | 页边距数值、标签列宽、区块属性数值、页眉内容文本 |
| 3 | **搜索框 Search** | 状态：同 Input；空态显示 placeholder `搜索记录` / `搜索字段`<br>有值时右侧出现 `✕`（16px）清空按钮 | 宽 240px（工具栏）/ 100%（字段池），高 32px | 工具栏搜索、字段池搜索、区块库搜索 |
| 4 | **分段控件 Segmented** | 2~3 段；状态：未选（底透明、文字 `#646A73`）/ 选中（底 `#EFF4FF`、文字 `#3370FF`、600）/ hover（底 `#F5F6F7`）/ disabled（文字 `#BBBFC4`） | 高 32px，`radius-control`，段内边距 12px | 编辑模式切换（卡片排版/文档排版）、密度三档、缩放预设、键值网格列数 |
| 5 | **下拉 Select** | 状态：default（同 Input）/ open（边框 `#3370FF`，箭头旋转 180°）/ 选项 hover（底 `#F5F6F7`）/ 选项选中（勾图标 `#3370FF`）<br>面板：`bg-panel`、`radius-card`、`shadow-card-hover`、最大高 320px 可滚 | 触发器高 32px；面板项高 32px | 视图名、纸张、方向、字体、字段选择器、页码格式 |
| 6 | **复选框 Checkbox** | 状态：未选（1px `#DEE0E3`，`radius 4px`）/ 选中（底 `#3370FF` + 白勾）/ 半选（横线）/ hover（边框 `#3370FF`）/ disabled（底 `#F5F6F7`） | 16×16，热区 32×32（含 label 区） | 显示分页边界、斑马纹、隐藏空值、页眉开关、页脚开关、页码开关 |
| 7 | **字段池项 FieldPoolItem** | 状态：default / hover（底 `#F5F6F7`）/ dragging（`opacity .6` + ghost）/ used（右侧 `✓` `#3370FF`）/ disabled（文字 `#BBBFC4` + ⊘ + tooltip）/ focus（2px `#3370FF` 外环） | 高 36px，宽 100%，`radius-control`，`☰` 16px | 卡片排版编辑器左栏 |
| 8 | **区块库项 BlockLibraryItem** | 同字段池项；额外有"组"容器（组标题可折叠，右侧 `▾`） | 高 36px；组标题高 28px | 文档排版编辑器左栏（5 组 12 类） |
| 9 | **卡片 Card** | 状态：default（`shadow-card-rest`、边框 1px 透明）/ hover（边框 `#3370FF`、`shadow-card-hover`）/ focus（2px `#3370FF` 外环）/ 高亮命中（2px `#F54A45` 左边框条）/ 骨架（灰条）/ disabled（编辑态：无 hover、不打开详情） | 最小宽 280px；`radius-card`；padding 12px 14px | S1 卡片墙 |
| 10 | **槽位 Slot** | 状态：浏览态（无边框）/ 编辑态（1px 虚线 `#DEE0E3` + 名称标签）/ 可落位（1px `#3370FF` + 底 `#EFF4FF`）/ 空（`拖字段到此处` 占位）/ 溢出（超出 maxItems 显示 `+n`） | 宽 100%；编辑态内边距 6px | 卡片四槽位（标题/副标题/属性/底部） |
| 11 | **纸页 Paper** | 状态：default（`shadow-paper`、`radius-paper`）/ 编辑态（同左 + 分页标尺）/ 空（中央提示 + 虚线投放区）/ 加载中（灰纸轮廓 + 3 条占位条） | **794×1123px**（A4 纵向），页边距默认上下 76 / 左右 68 | S2 预览、S4 编辑 |
| 12 | **区块 Block** | 状态：default（无装饰）/ hover（左 `⋮⋮` + 右 `✕` + 类型标签）/ selected（1px `#3370FF` + 浅底 + 类型徽标）/ dragging / invalid（warning 徽标 + 标红）/ 续页（`（续）` 标注） | 宽 = 内容区宽 658px；高度由内容决定 | 文档流内 12 类区块 |
| 13 | **抽屉 Drawer** | 状态：closed / open（250ms 滑入）/ dragging-width（把手激活）/ fullscreen / closing（200ms）/ printing（内部 zoom 复位、外表不变） | 默认宽 860px，可拖 560~视口宽；`shadow-drawer` | S2 详情 |
| 14 | **悬浮预览气泡 HoverPreview** | 状态：hidden / visible（150ms 淡入 + 上移 4px）/ 移入气泡（保持）/ 离开 200ms（淡出） | 宽 = 卡宽 + 32px；最大高 260px | S1 卡片 hover |
| 15 | **提示条 Banner** | 4 色（info/warning/error/success）；状态：default / hover（操作按钮 hover 底）/ 可关闭（`✕`）/ 不可关闭 | 高 40px，全宽 | 复制视图首开、存储降级、配置回退、分页降级、字段删除 |
| 16 | **徽标 Badge / 标签 Tag** | 变体：语义标签（4 色浅底 + 深色文字）/ 中性标签（底 `#F5F6F7` + 文字 `#646A73`）/ 计数徽标（底 `#F54A45` + 白字）/ 区块类型徽标（底 `#EFF4FF` + 文字 `#3370FF`）<br>状态：default / hover（描边） | 高 20px（标签）/ 16px（计数）；`radius-tag`；字号 12px | 单选/多选值、状态、字段类型标记、记录数、`（续）` |
| 17 | **骨架块 Skeleton** | 形状：卡片骨架（4 条灰条）/ 纸页骨架（灰纸轮廓 + 3 条）/ 文本行骨架 / 图片骨架（方形） | 灰条高 12px，`radius 4px`，间距 10px；底 `#F0F1F3`，微光 1400ms | 首屏、增量加载、分页中、图片加载中 |
| 18 | **空态 EmptyState** | 变体：无记录（插图 + 主副文案 + 可选按钮）/ 无字段 / 无区块（虚线投放区 + 提示）/ 详情无内容（纸内轻量空态） | 居中；插图区 64px；主文案 20px/500 `text-1`；副文案 14px `text-3` | 各空态场景 |
| 19 | **页码指示器 PageIndicator** | 状态：default（吸顶）/ 滚动中（同 default，值更新）/ 分页中（`正在分页…`）/ 全屏（同 default） | 高 32px，全宽；文案 13px `tabular-nums` | S2 抽屉顶部 |
| 20 | **页导航 PageNavigator** | 状态：default / hover（底 `#F5F6F7`）/ disabled（首页禁用 `‹`，末页禁用 `›`）/ 跳转输入聚焦 | 按钮 24×24；输入框宽 48px | S2 页码区（与指示器同一区域） |
| 21 | **Toast** | 变体：success / info / error；位置：顶部居中，距顶 24px；状态：in（200ms 淡入下移）/ out（200ms） | 高 36px，`radius-control`，`shadow-card-hover`，文字 13px | 保存成功、导出发起、错误轻提示 |
| 22 | **对话框 Dialog** | 变体：确认型（放弃修改/套用模板/重置模板）/ 告知型（配置超限）/ 冲突型（他人已修改） | 宽 400px，`radius-card`，遮罩 `rgba(0,0,0,.15)`；标题 16/600，正文 14px | 阻断性与二次确认操作 |
| 23 | **图标按钮 IconButton** | 状态：default（图标 `#646A73`）/ hover（底 `#F5F6F7`、图标 `#1F2329`）/ active（底 `#E5E6EB`）/ focus（2px 外环）/ disabled（`#BBBFC4`） | **32×32px** | 刷新、配置、更多、缩放 ±、全屏、打印、关闭 |
| 24 | **拖拽指示线 InsertionLine** | 状态：hidden / visible（2px `#3370FF`）/ 追加态（槽位底 `#EFF4FF`） | 高 2px，宽 = 容器内容宽 | 卡片槽位、文档流、区块行列表 |
| 25 | **幽灵元素 DragGhost** | 半透明副本，`opacity .6`，`shadow-drag-ghost`，`radius-control` | 与源元素同尺寸 | 所有拖拽操作 |
| 26 | **分页标尺 PageBoundary** | 状态：显示 / 隐藏；实线=强制分页，虚线=内容区底 | 线宽 1px（虚线 `#3370FF` 50%）；标签 11px `#8F959E` | S4 编辑画布（默认显示）、S2 预览（可选） |
| 27 | **调宽把手 ResizeHandle** | 状态：default（1px `#E5E6EB`）/ hover（1px `#3370FF`）/ dragging（1px `#3370FF`，光标 `col-resize`） | 命中区 8px × 100% | S2 抽屉左边缘 |
| 28 | **表格 Table（文档内）** | 状态：default / hover 行（底 `#FAFBFC`）/ 跨页续（重复表头 + `（续）` / 数值列右对齐） | 表头/行高 32px；`radius-paper` 内部无圆角 | 文档 `table` 区块 |
| 29 | **键值网格 KeyValueGrid** | 变体：1/2/3/4 列；状态：default / 斑马纹 / 含空值隐藏 | 行高 28px；标签列宽默认 96px；`breakInside: avoid` | 文档 `keyValueGrid` 区块（默认模板摘要区） |
| 30 | **字段清单 FieldList** | 状态：default / 可切分（按项）/ 空态（全部隐藏时整块不渲染） | 行高 32px；标签列宽 96px | 文档 `fieldList` 区块（默认模板详情区） |

---

## 8. 可访问性与可用性基线

| # | 基线 | 具体要求 | 验收方式 |
|---|---|---|---|
| A1 | **文本对比度** | 正文文本与背景对比度 **≥ 4.5:1**；大字号（≥18px 或 14px 加粗）≥ 3:1。已核对关键组合：`#1F2329` on `#FFFFFF` = 15.3:1 ✅；`#646A73` on `#FFFFFF` = 5.6:1 ✅；`#8F959E` on `#FFFFFF` = 3.6:1 ⚠️（**仅用于非关键辅助信息**，不得承载唯一语义）；`#BBBFC4` 仅用于禁用水位 | 用对比度工具抽查 S1/S2/S4 各 5 处 |
| A2 | **焦点可见** | 所有可聚焦元素在 `:focus-visible` 下显示 **2px `#3370FF` 外环 + 1px offset**，不得用 `outline:none` 无替代 | Tab 遍历 S1→S4 |
| A3 | **键盘可达** | ① Tab 顺序：工具栏 → 卡片区（逐卡）→ 抽屉（打开后焦点进入抽屉，`Tab` 在抽屉内循环）→ 关闭后焦点**回到原卡片**；② `Enter`/`Space` 打开卡片详情；③ `Esc`：关气泡 → 关抽屉（全屏时先退全屏）；④ 编辑态 `Alt+↑/↓` 移动区块、`Delete` 删除选中区块；⑤ 拖拽必须有**键盘替代**：字段池项聚焦后 `Enter` 进入"选择目标槽位"模式 | 全程仅用键盘跑完 §5.7 的 12 步主流程 |
| A4 | **点击热区** | 所有可点击元素热区 **≥ 32×32px**（图标按钮、复选框、`✕`、把手）；视觉尺寸可小于热区（用 padding 或伪元素扩展） | 抽查 10 个图标按钮 |
| A5 | **不依赖颜色单独传达信息** | ① 条件高亮除颜色外必须带文字标签或图标；② 状态标签（`● 进行中`）必须有文字；③ 错误/警告必须有图标 + 文案，不能只有红边框；④ 选中态除浅底外必须有描边或 600 字重 | 灰度截图后仍能辨别所有状态 |
| A6 | **文本缩放** | 浏览器缩放至 200% 时，S1/S2 无内容溢出、无横向滚动条（文档区允许出现内部滚动） | 浏览器 200% 缩放走查 |
| A7 | **语义与朗读** | 卡片使用 `role="article"` + `aria-label="记录：{标题}"`；抽屉 `role="dialog"` + `aria-modal="false"`（非模态）+ `aria-labelledby` 指向记录标题；纸页 `aria-label="第 1 页，共 2 页"`；图标按钮均有 `aria-label` | 屏幕阅读器（VoiceOver/NVDA）抽查 |
| A8 | **动效可关闭** | 尊重 `prefers-reduced-motion: reduce`：抽屉改为淡入（不做位移）、骨架微光停止、气泡改为即时显隐 | 系统开启减弱动效后走查 |
| A9 | **性能感知基线** | 抽屉滑入 250ms 内必须已有内容骨架（不能是空白面板）；预览刷新 ≤300ms；hover 气泡 150ms 延迟触发（防误触）；任何操作 100ms 内有视觉反馈 | 慢放录屏逐帧核对 |
| A10 | **可用性：错误可恢复** | 任何错误态必须提供"下一步动作"（重试/重新配置/查看备份/使用默认）；不得出现只能"关闭"的死路 | 逐个走查第 6 章 16 条状态 |

---

## 9. ⚠️ 与既有文档的差异点（需用户确认的设计决策）

> 本章是**评审重点**。以下每一条都与 `01-PRD.md` 或既有实现约定存在差异/留白，需要甲方明确拍板。

### D-UI-1 配置态形态变更：从「360px 配置抽屉」到「沉浸式三栏编辑模式」

| 项 | 内容 |
|---|---|
| 既有文档 | `01-PRD.md` §6.1 ③ 与 §6.2：**配置抽屉从右侧滑出、宽约 360px**；卡片区仍可见并进入编辑态；抽屉内装字段池 + 槽位配置 + 模板 + 主题 + 高亮 |
| 变更原因 | ① PRD 原方案是"卡片排版单模式"，而 03 文档引入了**文档排版编辑器**（区块库 12 类 + A4 画布 + 属性面板）；② A4 纸宽 794px，**360px 抽屉内根本放不下**；③ 三栏结构（220 + 画布 + 300/320）合计最小约 **1300px**，塞进 360px 无解 |
| 设计改为 | **沉浸式三栏编辑模式**：顶栏（48px，含返回/标题/模式分段/取消/保存）+ 左栏（字段池或区块库）+ 中栏（弹性预览/画布）+ 右栏（属性，300 or 320） |
| 影响 | ① 进入配置态时**卡片墙被隐藏**（不再"边看卡片边配置"）；② PRD §6.2"编辑态下卡片区仍可见但交互降级"这条**不再适用**；③ 需要新增"顶栏模式切换"这一元素，PRD 中无此元素 |
| 建议默认 | **采用沉浸式三栏**（否则 A4 文档排版无法实现） |
| 备选（若甲方坚持保留卡片墙） | 卡片墙在左（≥600px）、编辑器在右（≥860px）——1280 视口下不可行，仅在 ≥1600px 宽屏可用，且需另做一套响应式规则 |
| 待确认 | R1 |

### D-UI-2 详情抽屉默认宽度 860px

| 项 | 内容 |
|---|---|
| 既有文档 | `01-PRD.md` §6.3 写"右侧滑出详情抽屉（**宽约 480px**）"；`03-开发设计文档.md` §4.3 `DrawerConfig.defaultWidthPx` 取值 **860** |
| 变更原因 | 480px 与"**Word 打印预览**"语义冲突：A4 纸 794px，480px 内只能"适应宽度"缩到 ~60%，正文 14px 实际显示为 8.4px，不可读也不像打印预览 |
| 设计取值 | 默认 **860px**（= 794 纸宽 + 48 画布左右内边距 + 把手），在 1280 视口下占 67%；可拖 560px ~ 视口宽；支持全屏 |
| 影响 | ① 1280 视口下卡片区仅剩约 420px（约 1 列）被遮挡 → **阅读"卡片墙"的上下文被打断**；② 1920 视口下抽屉占 45%，体感可接受 |
| 建议默认 | **采用 860px**，并**记住用户上次拖拽的宽度**（下次打开沿用）；**不默认全屏**（避免每次都"夺屏"） |
| 待确认 | R2 |

### D-UI-3 卡片信息密度：属性区默认行数 + 三档密度

| 项 | 内容 |
|---|---|
| 既有文档 | `01-PRD.md` 未规定属性区默认行数；`03-开发设计文档.md` §4.4 有 `DensityConfig`（cardMinWidth / gap / padding / maxCardHeight），无三档命名 |
| 设计取值 | ① 属性区**默认最多 3 行**（超出按 `maxItemsPerCard` 截断，末尾显示 `+n`）；② 提供 **紧凑 / 标准 / 宽松** 三档密度，映射关系如下 |
| 三档映射 | 紧凑：`cardMinWidth 240 / gap 12 / padding 10 / maxCardHeight 160`，属性 2 行<br>标准（默认）：`cardMinWidth 280 / gap 16 / padding 16 / maxCardHeight 220`，属性 3 行<br>宽松：`cardMinWidth 320 / gap 20 / padding 20 / maxCardHeight 320`，属性 5 行 |
| 影响 | 1280 视口下：紧凑 5 列 / 标准 4 列 / 宽松 3 列。三档会显著改变"一屏信息量"的观感 |
| 建议默认 | **标准档 + 属性区 3 行**；密度三档保留在配置态右栏 |
| 待确认 | R3 |

### D-UI-4 卡片是否显示封面图 / 负责人头像

| 项 | 内容 |
|---|---|
| 既有文档 | `01-PRD.md` §7 提到附件字段卡片区"首图缩略图 + 附件数"、人员字段"头像 + 姓名"，但**未规定"卡片是否默认带封面图区"** |
| 设计取向 | ① **封面图默认不显示**（卡片不新增"封面槽位"）——理由：四槽位模型已定（标题/副标题/属性/底部），新增封面会破坏模型且让卡片高度不可控；需要图片时可在"属性区"放附件字段（渲染为首图缩略 64×64 + 张数）或使用"大图"模板；② **负责人头像默认显示**（若属性区含人员字段，渲染 20px 圆形头像 + 姓名） |
| 影响 | 卡片视觉更"轻"，但与"大图模板"的预期可能不符 |
| 建议默认 | 封面图**不显示**；头像**显示**；"大图"模板用"属性区首项改为大尺寸首图"实现 |
| 待确认 | R4 |

### D-UI-5 纸张缩放默认「适应宽度」+ 分页边界提示默认开关

| 项 | 内容 |
|---|---|
| 既有文档 | `03-开发设计文档.md` §4.3 `defaultFitToWidth: true`；无"分页边界提示"的默认值规定 |
| 设计取向 | ① 默认**开启「适应宽度」**：打开抽屉即看到整张纸的宽度，无需手动缩放（860px 抽屉下 zoom ≈ (860−48)/794 ≈ **1.02**，即接近 100%，体验最佳）；② 分页边界提示：**编辑态默认开、浏览态默认关** |
| 影响 | 若视口很窄（如抽屉被拖到 560px），"适应宽度"会把 zoom 压到 ~0.65，正文变小；需要"最小 zoom 下限 0.75，低于则改为居中显示并允许横向滚动"的保护 |
| 建议默认 | 采用上述；加"zoom 下限 0.75"保护 |
| 待确认 | R5 |

### D-UI-6 设计 token 中"推导值"的确认

| 项 | 内容 |
|---|---|
| 情况 | 主理人给定的视觉基线中，以下几项**未给具体值**，本文件按飞书观感推导补全，需要甲方确认 |
| 推导项 | ① 语义色浅底：success `#E8F8E6` / warning `#FFF4E6` / error `#FEECEB` / info `#EFF4FF`；② 卡片标题专用字号 `15px/600`（不在 11/12/13/14/16/20/22/28 字阶内）；③ hover 卡片边框使用主色 `#3370FF`（1px）；④ 灰度骨架底 `#F0F1F3`；⑤ 表格斑马纹 `#FAFBFC` |
| 影响 | 若与飞书原生标签色/组件色有出入，会造成"色差感"，属于与宿主融合原则（P-2）的直接相关项 |
| 建议默认 | 采纳上述推导值；**在 S6 设计规范页与飞书原生界面并排比对后再最终锁定** |
| 待确认 | 并入 R8 |

### D-UI-7 编辑态下卡片墙是否保留（D-UI-1 的子决策）

| 项 | 内容 |
|---|---|
| 设计取向 | 配置态**隐藏卡片墙**，编辑器全屏 |
| 权衡 | 保留卡片墙可提供"真实数据上下文"，但与 A4 画布争抢宽度 |
| 建议默认 | 不保留（与 D-UI-1 一致）；折中方案：顶栏提供"预览真实卡片"按钮，弹出一层只读浮层（可后置到 P1） |
| 待确认 | R6 |

---

## 10. 评审清单（UI-01 ~ UI-20）

> 使用方式：打开 `prototype/index.html`，逐条对照**对应屏幕**核验，在「结论」列填 **通过** 或 **需修改**（需修改请备注原因/期望）。

| 编号 | 评审项 | 所属屏幕 | 设计意图 | 判定标准 | 结论 |
|---|---|---|---|---|---|
| **UI-01** | 整体风格与宿主融合 | S1 / S6 | 让用户感觉不到"进了插件"（P-2） | 主色、圆角、字体、控件高度与飞书多维表格并排时"像同一产品"；无自创插画风格 | |
| **UI-02** | 卡片四槽位结构与收合 | S1 | 标题/副标题/属性/底部四槽位承载业务信息（P0-02） | 四槽位视觉可辨；空槽位自动收合不占位；内分隔线仅在需要时出现 | |
| **UI-03** | 卡片 hover 态与悬浮预览气泡 | S1 | 卡片不位移、气泡延迟 150ms 触发（P1-01 / US-6 AC3） | hover 只变边框与阴影；气泡含标题+副标题+3 属性+引导语；移入气泡不关闭 | |
| **UI-04** | 工具栏元素与信息层级 | S1 | 一屏内完成查看/搜索/筛选/进配置（P1-08） | 8 个元素（视图名/记录数/搜索/筛选/排序/刷新/配置/更多）排列清爽；筛选 chip 有值时有视觉区分 | |
| **UI-05** | 详情抽屉默认宽度与调宽 | S2 | 容纳 A4 纸的"打印预览"语义（P0-15，见 R2） | 默认 860px；8px 热区可拖 560~全屏；全屏可还原；`Esc` 两级（退全屏→关抽屉） | |
| **UI-06** | A4 纸张与页眉页脚呈现 | S2 | 第一眼读作"纸"而非"面板"（P-3） | 灰画布 + 白纸 + 纸影 + 2px 圆角；页眉/页脚 12px `#8F959E` + 1px 分隔线；纸页间距 24px | |
| **UI-07** | 键值网格（摘要区）排版 | S2 | 短值结构化信息一屏读完（默认模板 2 列 6 项） | 2 列网格；标签 `#646A73` / 值 `#1F2329`；行高 28px；不可跨页切分 | |
| **UI-08** | 字段清单（详情区）排版 | S2 | 混合长短值的完整展示区 | 单列纵向；标签列宽 96px 对齐；行高 32px；可按项跨页切分 | |
| **UI-09** | 跨页表格与续页标记 | S2 | 长表格跨页后仍可读（P0-13） | 按行切分；每页重复表头；续页右上标 `（续）`；不出现"只有表头无数据行"的孤页 | |
| **UI-10** | 页码与缩放一致性 | S2 | 屏幕与打印同源；缩放不影响分页（P0-14） | 纸内右下页码 + 抽屉吸顶指示器同值；zoom 50%/100%/150% 下**页数与断点完全一致** | |
| **UI-11** | 卡片排版编辑器三栏结构 | S3 | 拖拽排版需三栏才能承载（P0-03，见 R1） | 左 220 字段池（已使用/未使用/不支持三组）+ 中预览卡 + 右 300 属性；顶栏含模式切换与保存/取消 | |
| **UI-12** | 拖拽反馈强度 | S3 | 让用户始终知道"会落在哪" | 幽灵元素 + 目标槽位高亮 `#EFF4FF` + **2px 插入指示线**三重反馈；非法落点有禁止光标 | |
| **UI-13** | 文档排版编辑器三栏结构 | S4 | 区块化排版 A4 文档（P0-12） | 左区块库 + 中 794px A4 画布（含分页标尺）+ 右 320 属性/页设置/文档主题；区块 hover 与选中态可辨 | |
| **UI-14** | 区块库分组与命名 | S4 | 12 类区块可被快速找到（§5.2 五组） | 分组为 文本/字段/媒体/结构/元信息；中文命名直觉（尤其"键值网格/字段清单/标签行"三者可区分） | |
| **UI-15** | 页设置面板 | S4 | 纸张/边距/页眉页脚/页码可配（P0-11） | 6 项齐全；页边距以 **mm** 表达；改纸张/边距后画布尺寸即时变化 | |
| **UI-16** | 文档主题面板 | S4 | 字号阶梯/行高/间距/颜色可配 | 含字体、基础字号、行高、标题倍率 3 值、区块间距、首行缩进、4 色；改后 ≤300ms 重排 | |
| **UI-17** | 10 类状态视觉呈现 | S5 | 任何异常与降级都"可解释 + 可操作"（P-4） | 10 个场景逐个可见；文案可直接上线（无占位符/无内部术语）；降级态（4/5/9）不阻塞阅读 | |
| **UI-18** | 配色与字阶落地准确度 | S6 | token 与实现一致（第 3 章唯一来源） | 色板/字阶/间距/圆角/阴影逐项与 §3 数值一致；卡片标题 15px/600；数字为等宽 | |
| **UI-19** | 交互时长与性能感知 | 演示 | 动效克制、反馈即时（§3.7 / A9） | 抽屉 250ms、气泡 150ms、关闭 200ms；预览刷新 ≤300ms；任何操作 100ms 内有反馈 | |
| **UI-20** | 可访问性基线 | 全局 | 键盘可达、对比度达标、不依赖颜色（第 8 章） | Tab 顺序合理且可见焦点；`Enter` 开详情 / `Esc` 关抽屉 / 焦点归位；热区 ≥32×32；状态标签带文字 | |

**评审结论统计**：通过 ____ 条 / 需修改 ____ 条（由评审人填写）

---

## 11. 待拍板项汇总（R1 ~ R8）

> 每条给出「建议默认值」，若甲方无异议即按默认执行，无需逐条回复。

| 编号 | 决策项 | 选项 | **建议默认值** | 关联 |
|---|---|---|---|---|
| **R1** | 配置态形态：沉浸式三栏 vs 保留卡片墙 | A. 沉浸式三栏（卡片墙隐藏）<br>B. 保留卡片墙 + 编辑器并存（仅 ≥1600px 宽屏可用） | **A. 沉浸式三栏** | D-UI-1 / UI-11 / UI-13 |
| **R2** | 详情抽屉默认宽度 | A. 860px（+ 记忆用户上次宽度，不默认全屏）<br>B. 480px（PRD 原值，打印预览语义弱）<br>C. 默认全屏 | **A. 860px + 记忆宽度；不默认全屏** | D-UI-2 / UI-05 |
| **R3** | 卡片信息密度默认档与属性区行数 | A. 标准档 + 属性区 3 行（另提供紧凑/宽松）<br>B. 紧凑档 + 2 行<br>C. 宽松档 + 5 行 | **A. 标准档 + 3 行** | D-UI-3 / UI-02 |
| **R4** | 卡片封面图与负责人头像 | A. 封面图不显示 + 头像显示<br>B. 两者都显示<br>C. 两者都不显示 | **A. 封面图不显示（用"属性区首图 64×64"与"大图模板"替代）；人员字段显示 20px 头像 + 姓名** | D-UI-4 |
| **R5** | 纸张默认缩放与分页边界提示默认值 | A. 默认「适应宽度」+ 编辑态开/浏览态关；加 zoom 下限 0.75 保护<br>B. 默认 100%<br>C. 默认全开 | **A** | D-UI-5 / UI-10 |
| **R6** | 配置态下是否保留卡片墙可见 | A. 不保留（提供 P1 的"预览真实卡片"浮层）<br>B. 保留（牺牲 A4 画布宽度） | **A. 不保留** | D-UI-7 / UI-11 |
| **R7** | D4 复制视图首开提示的行为 | A. 顶部 info 提示条 + `从模板重配`/`立即配置` + `✕`（本会话不再提示）<br>B. 强制弹窗，必须选择后才能继续<br>C. 仅静默使用默认模板 | **A. 提示条 + 可关闭** | §6-16 / S5-6 |
| **R8** | 设计 token 推导值 & 存储降级提示强度 | A. 采纳 §3 全部推导值；存储降级提示常驻不可关闭<br>B. 采纳推导值；存储降级提示可关闭<br>C. 推导值待与飞书原生色并排比对后再锁 | **A**（推导值先采纳，S6 走查时若不匹配则按"以宿主为准"微调；存储降级提示必须常驻，因为它直接违反 US-4 AC2） | D-UI-6 / S5-5 |

---

## 12. 原型使用说明

### 12.1 如何打开

1. 打开文件：`C:\Users\drago\WorkBuddy\2026-09-20-11-00-29\feishu-card-view\prototype\index.html`
2. **直接用现代浏览器打开**（Chrome / Edge 新版）；无需启动任何服务、无需联网。
3. **浏览器窗口宽度请设为 ≥1280px**（建议 1440px），并确认系统缩放为 100%——原型按桌面 1280px+ 设计，移动端不在本次范围（D6）。
4. 原型为**纯静态高保真演示**：数据为内置示例，交互可点击但不连真实多维表格。

### 12.2 各屏怎么看

| 屏 | 入口 | 请重点看 |
|---|---|---|
| **S1** | 顶部或左侧导航选「浏览态 · 卡片墙」 | 工具栏排列、卡片四槽位、内分隔线、hover 卡片边框、悬浮气泡内容与位置、底部骨架卡 |
| **S2** ⭐ | 在 S1 点击任意卡片（或导航直接进入） | 抽屉宽度与推动画、灰画布 + 两张 A4 纸、页眉页脚、H1/H2、键值网格 2 列 6 项、字段清单 8 项、跨页表格的重复表头与 `（续）`、图片块、纸内页码 `1/2` 与吸顶指示器、拖把手调宽 |
| **S3** | 导航选「配置态 · 卡片排版编辑器」 | 三栏比例、字段池三组、预览样例卡、拖拽中状态（幽灵 + 槽位高亮 + 2px 指示线）、右栏五组配置、顶栏模式切换 |
| **S4** | 导航选「配置态 · 文档排版编辑器」 | 区块库 5 组 12 类、A4 编辑画布与分页标尺、区块 hover 与选中态、右栏区块属性/页设置/文档主题 |
| **S5** | 导航选「状态集」 | 10 个小场景逐个看，重点核对文案与降级表现（S5-4/5/6/9/10） |
| **S6** | 导航选「设计规范页」 | 与本文档 §3 的 token 表逐项对照（这是"设计是否被正确实现"的基准页） |
| **演示** | 导航选「交互演示」 | 按步骤串联，验证主流程连贯（约 60 秒） |

### 12.3 什么情况算"需要提修改"

以下任一情况出现，请在评审清单对应条目填「需修改」并描述期望：

| 情形 | 例子 |
|---|---|
| **布局不合理** | 三栏某栏太窄导致放不下；抽屉打开后卡片区完全无可读空间 |
| **信息层级错** | 卡片上最醒目的不是字段值而是按钮；文档区看不出"纸"的感觉 |
| **与宿主割裂** | 主色/圆角/控件高度与飞书多维表格明显不同 |
| **交互反馈缺失** | 拖拽时看不到会落在哪；点击后 100ms 内毫无变化 |
| **状态不可解释** | 出现灰色/空白但没有任何文案说明原因；错误态只能关闭 |
| **文案问题** | 出现内部术语（如 `keyValueGrid`）、占位符、错别字、与产品口径冲突（如写"编辑记录"但 MVP 是只读） |
| **与本文档规范不符** | 原型数值 ≠ 本文档 §3 的 token 值（此时以本文档为准，请在 UI-18 提出） |
| **A4 文档专业度不足** | 页边距过窄/过宽、行高拥挤、跨页断点割裂语义、表格缺表头 |
| **决策项不认可** | 对 R1~R8 的任一建议默认值有异议（直接在第 11 章标注即可，无需重复描述） |

### 12.4 不属于本次评审范围

- 真实数据性能（万行级首屏/滚动帧率）——需在真实多维表格环境验收（`03-开发设计文档.md` §13）。
- 打印/导出在飞书 iframe 沙箱内的真实可用性——属技术待验证项 V1。
- 移动端表现——本期明确不做（D6）。
- 配置持久化与跨用户共享行为——属技术待验证项 V4。
- 代码实现细节、接口定义、算法正确性——见 `03-开发设计文档.md`。

### 12.5 建议的评审顺序（30 分钟版）

1. 先看 **S6 设计规范页**（3 分钟）——确认视觉基线与本文档一致，这是后续所有判断的前提。
2. 再看 **S1 浏览态**（5 分钟）——确认"卡片墙"这个主入口成立。
3. 重点看 **S2 详情抽屉**（10 分钟）——本产品核心，也是分歧最可能集中处（R2）。
4. 然后 **S3 + S4**（8 分钟）——确认"沉浸式三栏"可接受（R1/R6）。
5. 最后 **S5 状态集**（4 分钟）——确认异常态文案与降级策略。

---

## 附录 · 与上游文档的对应关系

| 本文件章节 | 上游来源 |
|---|---|
| §2 设计原则 | `01-PRD.md` §2 产品目标、价值主张 V1~V5 |
| §3 Design Tokens | 主理人给定的视觉基线（原样采用）+ 本文件 §9 D-UI-6 的推导值 |
| §4 屏幕清单 | 主理人给定的 6 屏 + 1 演示清单；关联需求取自 `01-PRD.md` §5 与 §11.2 |
| §5.1 S1 | `01-PRD.md` §6.1 / §6.2 / §6.3；`03-开发设计文档.md` §6.5 |
| §5.2 S2 ⭐ | `01-PRD.md` §11.2（D3 详情区修订）、附录 B；`03-开发设计文档.md` §4.3 / §5.2~§5.6 |
| §5.3 S3 | `01-PRD.md` §6.2（拖拽流程、编辑态元素对照）；`03-开发设计文档.md` §10（双模式编辑器） |
| §5.4 S4 | `01-PRD.md` §11.2（P0-12）；`03-开发设计文档.md` §5.2 区块库、§10 文档排版模式 |
| §5.5 S5 | `01-PRD.md` §6.5；`03-开发设计文档.md` §12（15 类异常/降级态） |
| §6 状态与反馈 | `03-开发设计文档.md` §12 全表 + `01-PRD.md` §9 Q5/D4（复制视图首开） |
| §7 组件清单 | `03-开发设计文档.md` §14 目录结构中的组件清单 + 本文件第 3 章 token |
| §8 可访问性 | `01-PRD.md` §8 非功能性需求（键盘/对比度/语义） |
| §9 差异点 | `01-PRD.md` §6.1/§6.3（360px、480px）与 `03-开发设计文档.md` §4.3（860px、适应宽度）、§4.4（DensityConfig）的冲突与留白 |
| §11 R1~R8 | 由 §9 差异点 + §5 各屏「评审关注点」中预判可能被否的项收敛而来 |

### 附录 B · 本文件引用的上游需求编号

| 编号 | 需求名 | 本文件主要落点 |
|---|---|---|
| P0-01 | 卡片视图基础渲染 | §5.1 |
| P0-02 | 卡片槽位结构 | §5.1.2、§5.3.2 |
| P0-03 | 拖拽式排版编辑器 | §5.3 |
| P0-04 | 实时预览（≤300ms） | §3.7、§5.3.3、§5.4.3、UI-19 |
| P0-05 | 配置持久化（v2 + D4 提示） | §6-16、S5-6、R7 |
| P0-06 | 卡片展开详情（文档式） | §5.2 |
| P0-07 | 字段类型适配基础集 | §5.1.5、S5-8 |
| P0-08 | 浏览态/配置态切换（双模式） | §5.3、§5.4、§4.1 屏幕地图 |
| P0-09 | 基础性能与虚拟化 | S5-2、S5-4、A9 |
| P0-10 | 空态与异常态 | 第 6 章 16 条、§5.5 |
| **P0-11** | 文档式详情渲染 | §5.2.2、§5.2.6 |
| **P0-12** | 文档区块排版编辑器 | §5.4 |
| **P0-13** | 分页引擎 | §5.2.6（分页边界、跨页表头、续页） |
| **P0-14** | 打印与导出 PDF | §5.2.5、§6-13 |
| **P0-15** | 详情预览控制 | §5.2.3、UI-05、UI-10 |
| P1-01 | 悬浮预览 | §5.1.4、UI-03 |
| P1-02~P1-05 | 模板/主题/密度/条件高亮 | §5.3.2、D-UI-3 |
| P1-08 | 对齐原生筛选/排序 | §5.1.2（筛选 chip、记录数） |
| D4 | 复制视图首开提示 | §6-16、S5-6、R7 |
| D6 | 移动端本期不做 | §12.4 |
| D9 | 配置体积 ≤64KB | §6-14、S5-10 |

---

**本文档状态：待评审。** 评审通过后，请将第 10 章结论列填写完整，并在第 11 章确认 R1~R8；逾期未确认的项按「建议默认值」执行。