node_modules/
dist/
coverage/
*.log
npm-debug.log*
yarn-error.log*
.DS_Store
.idea/
.vscode/
*.local

# 一次性验证/诊断产物（团队约定：**不删除、仅忽略**）。
# 限定在工程根目录（`/_*`），不会波及 src/ 或 public/ 下的下划线文件。
/_*

# QA 轮次的一次性验证产物（同为**不删除、仅忽略**）。
# 例：变异验证的 hash 快照、逐档位断言结果、临时解析脚本等。
# 这些是排查过程的中间产物，不是可复现的交付物；正式测试全部在 src/**/*.test.* 内。
qa2b_*
qa2_out/
qa_out.txt
